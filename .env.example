# Root — copy to per-package .env files as needed (never commit real .env)

# Backend (see backend/.env.example)
# Document service (see document-service/.env.example)
# Frontend (see frontend/.env.example)
