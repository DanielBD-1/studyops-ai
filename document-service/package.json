{
  "name": "studyops-document-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "StudyOps AI — document processing microservice",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "dev": "node --env-file=.env --watch src/server.js",
    "test": "node --test tests/health.test.js tests/unit/env.test.js"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "zod": "^4.4.3"
  }
}
