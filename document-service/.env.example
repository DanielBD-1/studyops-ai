NODE_ENV=development
PORT=3002

# Gemini — required for POST /process (server-side only; never commit real keys)
GEMINI_API_KEY=your_gemini_api_key_here

# Model ID for generateContent (optional; default gemini-2.5-flash-lite)
# Example fallback: gemini-2.5-flash
GEMINI_MODEL=gemini-2.5-flash-lite
