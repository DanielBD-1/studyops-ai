# Current State — StudyOps AI

**Purpose:** Short starting point for the owner and for agents. Read this first, then drill into linked docs.

**Last updated:** 2026-06-07 (Phase **DOCS-FLASHCARD-REVIEW-A3** — **FLASHCARD-REVIEW-A3** frontend review-state filter on saved DB flashcards documented; **`filterFlashcardsByReviewState`**; global **`/flashcards`** + material detail saved DB sections; client-side by **`mastery`**; **no** backend **`?mastery=`**; **no** URL-persisted filters; **not** full SRS/**`next_review_at`**; **FLASHCARD-REVIEW-A2** Known/Unknown unchanged; plan JSON **unchanged**; **FLASHCARD-REVIEW-A4** deferred; Supervisor **approved with notes**; main CI **green**; prior **DOCS-FLASHCARD-REVIEW-A2** — **FLASHCARD-REVIEW-A2** documented; prior **DOCS-FLASHCARD-REVIEW-A1** — **FLASHCARD-REVIEW-A1** backend documented; prior **DOCS-TASK-MATERIAL-DETAIL-A3** — **TASK-MATERIAL-DETAIL-A3** material detail **Related study tasks** read-only band documented; prior **DOCS-TASK-MATERIAL-FILTERS-A2** — **TASK-MATERIAL-FILTERS-A2** frontend study material task filters documented; prior **DOCS-TASK-MATERIAL-LINKS-A1** — **TASK-MATERIAL-LINKS-A1** **`TaskCard`** material navigation links documented; prior **DOCS-CONSISTENCY-FIX** — stale Trello OAuth/A5 deferred language removed from **PRD**, **README**, **IMPLEMENTATION_STATUS**, **AGENT_MEMORY**; prior **DOCS-TRELLO-OAUTH-A5C** — Trello OAuth **A5C** backend manual-credential hardening documented; connected users must use stored-token mode or disconnect before manual credentials; **`TRELLO_MANUAL_CREDENTIALS_NOT_ALLOWED`** / **400** when connected + manual `{ apiKey, token }`; manual fallback remains available/collapsed when **disconnected**; **A5B** connected-account sync remains primary **`/trello` UX**. Prior **DOCS-TRELLO-OAUTH-A5B** — **A5B** frontend connected-account sync; prior **DOCS-TRELLO-OAUTH-A5A** — **A5A** backend stored-token mode; deferred optional follow-ups remain **separate-gated** — **not automatic**)

---

## Product platform (web only)

**StudyOps AI is a browser-based web application only.** It is **not** a mobile app, native mobile app, Android/iOS app, phone app, app-store product, or mobile-first/native product.

- **In scope:** The same React web app in desktop and mobile **browsers**; **responsive web layout** at narrow browser widths.
- **375px QA:** References to **~375px** width in recent phases and follow-up QA mean **narrow responsive browser viewport** / **small viewport web layout** testing — **not** mobile-app product scope.
- **Terminology for agents:** Use **narrow responsive browser layout**, **small viewport web layout**, or **responsive web viewport** — not “mobile app”, “native mobile”, “phone app”, “mobile direction”, “mobile navigation”, “bottom tabs”, or “app-store flow”.
- **Stitch / DESIGN:** Visual inspiration for the **web UI only** — does not approve native mobile work, sidebar implementation (unless separately approved), or mobile-specific navigation / app-store patterns. **`docs/design/studyops_ai_visual_style_guide_v2_2.md`** (Stitch Visual Style Guide **v2.2**) is a **reference artifact only**; **`DESIGN.md`** remains the official project design document.
- **Stitch visual guide v2.2 (DOCS-I8F):** Added as reference artifact at **`docs/design/studyops_ai_visual_style_guide_v2_2.md`**. **BX-I8E** shared controls / card surfaces alignment check **passed** against it. **BX-I8F** visual smoke audit **passed with notes**. **BX-I9B1** radius token alignment **complete** (commit **`9ec2917`**). **BX-I9B2a** canvas/shell color token alignment **complete** (commit **`f92cbda`**). **BX-I9B2b** primary/cyan color token alignment **complete** (commit **`b4d7b93`**). **BX-I9B2c** AI/violet color token alignment **complete** (commit **`19d444e`**). **BX-I9B2d** danger/error color token alignment **complete** (commit **`ae5cfc8`**). **BX-I9C** final visual QA **passed with notes** (documentation-only gate — **no** repo file changes). **BX-I9C-Auth** authenticated visual QA **passed with notes** on **`http://localhost:5173`** (Vite dev — **not** `vite preview` at **`127.0.0.1:4173`**). **BX-I10A1** flashcard study glass polish **complete** (commit **`e62c1b0`** — **`components.css`** only; logged-in visual smoke **Pass with notes**). **BX-I10A2** material-only AI Generate gradient **complete** (commit **`b90108e`** — **`components.css`** only; scoped violet→blue gradient on material **Generate study plan** CTA; global **`.btn--primary`** unchanged; **BX-I10A2-SMOKE** **Pass with notes**). **BX-I10A3** filter-toolbar / command-surface glass unification **complete** (commit **`cb54ec5`** — **`layout.css`** only; global **`.filter-toolbar`** glass/inset surfaces; segmented filter tracks; shared quiet-glass filter-empty strips; tasks/flashcards true empty-state downgrades neutralized; **BX-I10A3-SMOKE** **Pass with notes**). **BX-I10A4** course/document accent rail consistency **complete** (commit **`7e5e61f`** — **`components.css`** + **`layout.css`** only; **6px** course identity rails on course surfaces; **3px** muted document-shelf hint on course detail via **`color-mix`**; materials primary **4px** top rule; **no** JSX/**`StudyMaterialDetail.jsx`**/**`tokens.css`**/**`base.css`**/backend/package changes; Supervisor **Approve with notes**; Security **Pass with notes**; **`npm.cmd run lint`**, **`npm.cmd test`**, **`npm.cmd run build`** passed; **BX-I10A4-SMOKE** **Pass with notes** at **~375px** / **1280px** — **narrow responsive browser viewport**, **not** native mobile). **BX-I10A5** targeted glass/elevation pass **complete** (commit **`38aa561`** — **`components.css`** + **`layout.css`** only; softened navigable-card hover — reduced elevation, no lift transform; quiet inline glass on tasks create/edit, flashcards library/create, Trello wizard/sync/results steps, course settings/create-material/materials empty; optional Trello picker row polish + Focus session outer panel softening; **BX-I10A1**–**BX-I10A4** protected areas + dashboard hero + material AI cockpit + material editor/library elevation **unchanged**; Supervisor **Pass with notes**; Security **Pass with notes**; **`npm.cmd run lint`**, **`npm.cmd test`**, **`npm.cmd run build`** passed; **BX-I10A5-SMOKE** **Pass with notes** on **`http://localhost:5173`** at **~375px** / **1280px** — **narrow responsive browser viewport**, **not** native mobile). **BX-I10A6-QA** Trello failed-row live verification **complete** (**review only** — **Pass with notes** on Vite dev **`http://localhost:5173`**; route **`/trello`** at **~375px** / **1280px**; failed sync row triggered live with harmless fake Trello API key/token — **no** real credentials, **no** Trello cards created; backend **HTTP 401** / **`TRELLO_AUTH`** / structured **`trello_api_error`** only; Step 5 **Sync results** failed row/badge/error text readable; danger/error styling distinct from course rose and from success green; Trello regressions held; credentials **`type="password"`**, cleared after sync; **no** fix phase required). Deferred optional follow-ups (each requires a **separate phase gate**): optional material editor/library elevation alignment; optional global shadow/token cleanup only if explicitly approved; **ROADMAP-A1** background automations + AWS architecture planning; optional material detail course accent JSX if explicitly approved; course accent width alignment; Hanken Grotesk font only with separate font approval; narrow nav focus / material source-type pill polish only if separately approved.
- **BX-I6 / B4:** Future visual phases must **not** expand scope into mobile/native/app-store products without explicit human approval.

---

## Current stable state

StudyOps AI is an **actively developed web product**: initial MVP baseline **complete**; current product state continues through post-MVP phases (plan history, Trello OAuth **A2–A6**, presentation polish, etc.). Stack: React frontend → Express backend → document-service (Gemini) → Supabase Auth + Postgres. CI runs lint, tests, and frontend build on Node.js 22. **Production deployment** (cloud/hosted) remains **deferred** — local dev + CI only unless separately approved.

**UI maturity (read before planning visual work):** The functional core and many presentation hardening phases are **complete** (**B4-F3A** / **B4-F3B** / **B4-F3C**, **BX-I2**–**BX-I6D**, **B4-A**–**B4-F3C3**, **BX-I7B**, **BX-I7C**, **BX-I7D Tier 1**, **BX-I7E1**, **BX-I7E2**, **BX-I7E3 Tier A**, **BX-I7E4**, **BX-I7F**, **BX-I8B**, **BX-I8C**, **BX-I8D**, **BX-I8E**). That work improved dark/glass tokens, state surfaces, page polish, accessibility, and cross-route visual consistency — but the UI is **not** final **Stitch-level** presentation yet. Phase **BX-I7A** (documentation audit) recorded a **desktop WEB layout width gap** at the prior **1120px** global cap. Phase **BX-I7B** (**complete**, commit **`00d3255`**) widened **`--content-max-cockpit`** and **`--content-max-shell`** from **1120px** to **1280px** in **`frontend/src/styles/tokens.css`** and **`frontend/src/styles/layout.css`** only — **1120px** is **no longer** the current production cap for cockpit/shell; **`--content-max-form`**, **`--content-max-workspace`**, and **`--content-max-reading`** were **not** changed; **`AppShell`** and **`page--cockpit`** stay aligned; desktop-only **`.page`** horizontal padding at **`min-width: 1280px`**; **narrow responsive browser viewport ~375px** behavior preserved. **BX-I7B** is a **foundation step only** — it does **not** fully solve the Stitch/design gap. Phase **BX-I7C** (**complete**, commit **`583922d`**) — **CSS-only** dashboard desktop grid expansion in **`frontend/src/styles/layout.css`** only: at **`min-width: 1280px`**, **PageHeader** spans full width; **dashboard-hero** and **dashboard-study-pulse** sit side-by-side (hero spans full width when study pulse absent); **dashboard-courses** and **dashboard-secondary** span full width below; **dashboard-course-list** becomes a 2-column grid on desktop; loading/error-only dashboard states remain unaffected (`:has(.dashboard-hero)`); **narrow responsive browser viewport ~375px** remains stacked; **no** **`DashboardStub.jsx`**, API/backend/database/service/context/recommendation/copy changes; **no** fake metrics, charts, sidebar, or mobile/native UI. **BX-I7C** improves Dashboard desktop density but does **not** complete full Stitch-level UI. Phase **BX-I7D Tier 1** (**complete**, commit **`52c68ab`**) — **CSS-only** courses/course detail desktop shelf expansion in **`frontend/src/styles/layout.css`** only: at **`min-width: 1280px`**, **`/courses`** course shelf **3-column** grid; **`/courses/:id`** populated document/material shelf **2-column** grid; loading/error/empty states unaffected; tasks remain below materials unchanged; settings form, create material form, and danger zone unchanged; **narrow responsive browser viewport ~375px** remains stacked; **no** **`CoursesList.jsx`**, **`CourseDetail.jsx`**, component, API/backend/database/service/context/recommendation/copy changes; **no** fake metrics, charts, sidebar, or mobile/native UI; **Tier 2** (materials | tasks side-by-side workspace, **`.course-workspace`** full-page grid, DOM/CSS reorder) **not implemented**. **BX-I7D Tier 1** improves Courses/Course Detail desktop density but does **not** complete full Stitch-level UI. Phase **BX-I7E1** (**complete**, commit **`d0db43e`**) — **CSS-only** Tasks desktop panel expansion in **`frontend/src/styles/layout.css`** only: at **`min-width: 1280px`**, **`/tasks`** command controls use a horizontal desktop band (filters use available width; toolbar / Add study task aligns right when space allows); populated task list **2-column** desktop grid (`:has(.task-workspace__list)`); create task FormCard remains full-width/readable; inline edit item intended to span full width via **`:has(.task-workspace__edit-card)`**; loading/error/empty/filter-empty states remain full-width/unaffected; **narrow responsive browser viewport ~375px** remains stacked; **no** **`GlobalTasksSection.jsx`**, **`TasksPage.jsx`**, **`TaskCard.jsx`**, component, API/backend/database/service/context/copy changes; **no** fake metrics, charts, sidebar, or mobile/native UI. **BX-I7E1** improves Tasks desktop density but does **not** complete full Stitch-level UI. Phase **BX-I7E2** (**complete**, commit **`b18304c`**) — **CSS-only** Flashcards desktop panel expansion in **`frontend/src/styles/layout.css`** only: at **`min-width: 1280px`**, **`/flashcards`** command controls use a horizontal desktop band (filters use available width; toolbar / Create flashcard aligns right when space allows); populated flashcard library uses **study | manage** **2-column** desktop layout (`:has(.flashcards-workspace__study-zone)` — study zone column 1, manage zone column 2); manage list **2-column** desktop grid when populated; create form and inline edit item remain full-width/readable (`grid-column: 1 / -1` on create block children and **`:has(form)`** on list items); intro/loading/error/empty/filter-empty/status/create-cta remain full-width; **`actionError`** remains outside **`.flashcard-library`** and unaffected; manage list still shows truncated questions only — **no** answers newly exposed in manage list; **narrow responsive browser viewport ~375px** remains stacked; **no** **`GlobalFlashcardsSection.jsx`**, **`FlashcardsPage.jsx`**, **`FlashcardStudy.jsx`**, component, API/backend/database/service/context/copy changes; **no** fake metrics, charts, sidebar, or mobile/native UI; **no** mastery score / AI score / memory score / health score. **BX-I7E2** improves Flashcards desktop density but does **not** complete full Stitch-level UI. Phase **BX-I7E3 Tier A** (**complete**, commit **`cba6dde`**) — **CSS-only** Trello desktop setup panel expansion in **`frontend/src/styles/layout.css`** only: at **`min-width: 1280px`**, **`/trello`** wizard flow grid on **`.trello-workspace__flow`** — Step 1 credentials + Step 2 destination side-by-side; Step 3 tasks, **`.trello-sync__messages`**, and Step 4 sync remain full-width; sync results stay below command band in **`.trello-workspace__results-zone`**; DOM step order unchanged (credentials → destination → tasks → messages → sync → results); **narrow responsive browser viewport ~375px** remains stacked; **no** Trello JSX/components/services/API/backend changes; **no** fake metrics, charts, sidebar, or mobile/native UI; **Tier B not implemented** — no 2-column task list, no results grid, no wizard/results side-by-side, no scroll-height override. **BX-I7E3 Tier A** improves Trello desktop density — does **not** complete full Stitch-level UI. Phase **BX-I7E4** (**complete**, commit **`467ccd9`**) — **CSS-only** Admin desktop stats panel expansion in **`frontend/src/styles/layout.css`** only: at **`min-width: 1280px`**, **`/admin`** stats deck **`.admin-workspace__stats-deck`** → **2-column** grid; Platform overview, Trello today (UTC), and Backend status bands remain full-width (`grid-column: 1 / -1`); Tasks|Focus and Learning|Trello **`.admin-workspace__stats-row`** blocks sit side-by-side as adjacent grid cells; inner **`.admin-workspace__stats`** stat grids unchanged; loading/error/forbidden/**`AdminRoute`** states unaffected (selectors target success-body deck only); **narrow responsive browser viewport ~375px** remains stacked; **no** **`AdminDashboardPage.jsx`**, API/backend/services/auth/copy changes; **no** fake metrics, charts, health scores, new KPIs, sidebar, or mobile/native UI; backend status remains existing honest **`stats.systemHealth.backend`** value only — **no** fake monitoring chrome. **BX-I7E4** improves Admin desktop density — does **not** complete full Stitch-level UI. Phase **BX-I7F** (**complete**, commit **`25988dc`**) — **CSS-only** material cockpit desktop pass in **`frontend/src/styles/layout.css`** only: at **`min-width: 1280px`** on **`/study-materials/:materialId`**, success-body cockpit **Source | AI** columns **1.15fr | 0.85fr** (`:has(.material-workspace__cockpit)`); AI **plan-history** / **plan-import** action rows horizontal with wrap; material flashcards library **study | manage** **2-column** when study zone present (`:has(.flashcard-library__study)`); manage list **2-column** when populated; create/inline edit full-width (`:has(form)`); loading/error/not-found/page-error shells unaffected; **375px** stacked; **`/flashcards`** unaffected; **no** JSX/API/services/backend/auth/AI behavior/logging/content-exposure changes; plan history list/preview behavior unchanged. **BX-I7** desktop layout sequence **now complete**: **BX-I7B**, **BX-I7C**, **BX-I7D Tier 1**, **BX-I7E1**, **BX-I7E2**, **BX-I7E3 Tier A**, **BX-I7E4**, **BX-I7F**. Optional deferred only: **BX-I7D Tier 2** (course detail materials | tasks side-by-side), **BX-I7E3 Tier B** (Trello task-list grid) — each **not automatic**. Authenticated visual smoke for **BX-I7F** **recommended before merge** if not yet done (optional — does **not** block commit). Authenticated visual smoke for **BX-I7E4** **recommended before merge** if not yet done (optional — does **not** block commit). Authenticated visual smoke for **BX-I7E3 Tier A** **recommended before merge** if not yet done (optional — does **not** block commit). Loading/error/**`actionError`** / console / formal keyboard tab smoke for **BX-I7E2** **recommended before merge** if not yet done (optional). Inline edit and console smoke for **BX-I7E1** **recommended before merge** if not yet done. Authenticated visual smoke for **BX-I7C** / **BX-I7D Tier 1** remains **recommended before merge** if not yet done. Do **not** claim Stitch is fully implemented, the full cockpit layout is final, or all desktop layout gaps are solved.

**Production UI today:** Dark graphite / glass token foundation from `frontend/src/styles/tokens.css` (Phase **BX-I2**, commit **`03ee9df`**) — electric blue primary, violet AI accent, cyan data accent, dark-friendly semantic states, source editor surface, filled-button WCAG AA contrast fix. Global **`AppShell`** top navigation / WEB dashboard chrome polished (**BX-I6D**, commit **`9252ba9`**) — **CSS-only** in **`layout.css`** only: stronger glass shell bar, restrained top accent strip, improved brand/nav active/hover/**`:focus-visible`** styling (active route **not** color-only), labeled **Log out** remains visible on the brand row at **narrow responsive browser viewport ~375px** with horizontal nav scroll (WEB top-nav row — **no** bottom tabs, drawer, hamburger-first, or phone-style UI); **`prefers-reduced-motion`** for shell transitions; **no** `AppShell.jsx`, auth, route guards, route fade, page transitions, or `useLocation` animation. Cockpit layouts, Source | AI material detail (**12A-1** + **BX-I5** visual polish, commit **`c2288d4`**), generated plan history (**11A-3**), dashboard **AI Study Command Center** presentation (**BX-I6B**, commit **`cceb4e0`**) on top of **rule-based** **“What should I study next?”** decision layout (Phase **BX-I3**, commit **`bdd6f2a`**) — uses existing **`GET /api/dashboard/stats`** only; flagship recommendation hero, glass/depth/glow via existing tokens, Study pulse cockpit band with factual Pending/Completed/Total metrics, richer course workload command deck, **At a glance** visually tertiary; **no** `dashboard-recommendation.js` logic changes; **no** fake AI confidence, urgency, priority, health score, or analytics; **no** chart libraries, sidebar, weekly focus chart, or new packages. **Deterministic course accents** (Phase **BX-I4**, commit **`ff65e21`**) — stable **`amber` | `rose` | `emerald`** mapping from existing course ID/name/title only; accent rail/pill on **`CourseCard`**, header accent on **`CourseDetail`**, workload-row accents on **`/dashboard`**; supplementary visual chrome only (**no** health score, priority, urgency, active/quiet state, or AI classification); **no** raw user strings as class names; **no** logging, persistence, backend/API, DB, or package changes. **Material cockpit visual polish** (**BX-I5**, commit **`c2288d4`**) — improved Source | AI hierarchy, darker readable source/editor well, source-type pill from existing **`sourceTypeLabel`**, AI command-column wrapper/dividers, polished generate/active-plan/history surfaces, plan scanability, import toolbar band, history preview inset, flashcards library consistency, responsive polish at existing breakpoints; **frontend/CSS/className-only** — **no** `tokens.css` change, **no** behavior/API/payload change, **no** unsafe rendering (material and plan content remain plain React text). **Material AI command surfaces polish** (**BX-I8B**, commit **`bda2645`**) — stronger violet/glass treatment on material-detail AI command column (generate panel, loading lane, plan output, history, import bands, cockpit AI shell); **Generate** CTA remains electric blue / primary; **`components.css`** + **`layout.css`** only — **no** `tokens.css`, JSX, API, services, backend, auth, routes, behavior, copy, logging, or content-exposure changes; **375px** stacked; **BX-I7F** desktop layout preserved; **no** fake metrics, charts, scores, sidebar, or mobile-native UI. **Auth + PageHeader intro chrome** (**BX-I8C**, commit **`8008dc1`**) — auth screens use glass/command-center **`.page--auth .form-card`** treatment; intro **`PageHeader`** uses glass intro band + top gradient rule (**.page-header--intro** only); **`.auth-brand`** gradient bar + soft glow on **`/`** and **`/register`**; course accent header on **`CourseDetail`** preserved (inset accent + **`elevation-1`**); **`components.css`** + **`layout.css`** only — **no** `tokens.css`, **`base.css`**, JSX, API, backend, auth, routes, services, behavior, copy, logging, or content-exposure changes; **375px** stacked layout preserved; **BX-I7** desktop grids and **BX-I8B** material AI cockpit preserved; **no** fake metrics, charts, scores, sidebar, or mobile-native UI; focus-visible clipping smoke on intro header actions **recommended but not blocking** (prior Security / Trust Review **PASS**). **Motion micro-pass** (**BX-I8D**, commit **`51cdc77`**) — **`frontend/src/styles/components.css`** + **`frontend/src/styles/base.css`** only; processing opacity pulse on **`.ai-panel__loading--active .loading`** only during real AI generate (**`generating`** + Processing Lane); short one-shot **`plan-fade-in`** on success status, plan history preview panel, and **`.focus-done`**; **`prefers-reduced-motion`** disables pulse, entrances, and press/hover transforms in **`base.css`**; **no** JSX/JS/API/backend/routes/tokens changes; **no** route transitions, **`App.jsx`**, **`useLocation`**, fake idle AI thinking, fake metrics/charts, or idle decorative infinite animation; **BX-I7** / **BX-I8B** / **BX-I8C** layouts unchanged; manual smoke **recommended before merge**, **not blocking** (prior Security / Trust Review **PASS**). **Shared controls / card surface alignment** (**BX-I8E**, commit **`52b7b78`**) — **`frontend/src/styles/components.css`** only — conservative shared primitive alignment: global **`.form-card`** baseline glass-border + subtle raised gradient; **`.source-card`** baseline glass-border only; **`.link-btn--primary`** / **`.link-btn--danger`** parity with **`.btn`** variants (unused in JSX until adopted); **`.link-btn--secondary:hover`** + **`.link-btn--disabled`** disabled/hover parity; **`.alert--success`** visual parity with **`.alert--error`**; **`layout.css`** / **`tokens.css`** / **`base.css`** untouched; **no** JSX/API/backend/routes/services/behavior/copy/logging changes; **no** fake metrics/charts/scores/sidebar/mobile-native UI; optional scope **not implemented** (no **`dashboard-empty-cta__link`**, empty/status block unification, command-band/hero/AI cockpit/auth PageHeader/BX-I7 grid edits); Visual Style Guide v2.2 alignment check **passed** — safe to continue; token/radius/font/AI-gradient gaps **deferred** at **BX-I8E** time; manual smoke **recommended before merge**, **not blocking** (prior Security / Trust Review **PASS**). **Radius token alignment (BX-I9B1, commit `9ec2917`):** Stitch Round Eight in **`tokens.css`** + **`components.css`** only — **`--radius-sm`**: **8px**; **`--radius-md`**: **12px**; **`--radius-lg`** / **`--radius-xl`** unchanged; **`.btn`** / **`.link-btn`** use **`--radius-md`**; **`.field__*`** remain **`--radius-sm`**; **no** color/shadow/spacing/typography token changes; **`layout.css`** / **`base.css`** untouched; **no** JSX/API/backend/routes/services changes; **no** AI Generate gradient; **no** flashcard-study polish; indirect cascade to pills/cards via shared radius tokens **expected**; authenticated visual smoke **recommended before merge**, **not blocking** (prior Security / Trust Review **PASS**). **Canvas/shell color alignment (BX-I9B2a, commit `f92cbda`):** Stitch v2.2 canvas/shell environment tokens in **`tokens.css`** only — **`--color-bg`** **`#0F172A`**; **`--color-bg-subtle`** harmonized (**`#1a2332`**); **`--color-bg-auth`** gradient harmonized; **`--color-shell-bg`** toward **`#0B0F1A`** with translucency preserved; **`--color-shell-border`** slightly softened; **no** **`--radius-*`** changes; **no** primary/AI/cyan/danger/focus/glow/shadow/surface/text/editor/chart/course-accent token changes; **`components.css`** / **`layout.css`** / **`base.css`** untouched; **no** JSX/API/backend/package changes; **no** AI Generate gradient; **no** flashcard-study polish; **no** glass/elevation pass; indirect cascade via existing **`var(--color-bg*)`** / **`var(--color-shell-bg)`** in **`layout.css`** **expected**; Supervisor Review **PASS**; Security / Trust Review **PASS**; authenticated visual smoke **recommended before merge**, **not blocking**. **AI/violet color alignment (BX-I9B2c, commit `19d444e`):** Stitch v2.2 AI Accent **`#8B5CF6`** in **`tokens.css`** — **`--color-ai-accent`** **`#8b5cf6`**; AI rgba family re-based to **rgb(139, 92, 246)**; **`--glow-ai`** **`rgba(139, 92, 246, 0.2)`**; mechanical hardcoded lavender sweep in **`components.css`** (**20** replacements) and **`layout.css`** (**3** replacements) — **`rgba(208, 188, 255, α)` → `rgba(139, 92, 246, α)`** with alphas preserved; stale lavender check **`rg "208,\s*188,\s*255|#d0bcff" frontend/src/styles`** — **no matches**; **no** selector/layout/spacing/motion/border-width changes; **no** radius/canvas/shell/primary/cyan/danger/error changes; **no** JSX/API/backend/package changes; **Generate** CTA remains primary blue; Supervisor Review **PASS**; Security / Trust Review **PASS**; authenticated visual smoke **recommended before merge** (material AI cockpit **375px** / **≥1280px**, dashboard hero violet tint, AppShell accent strip, auth brand gradient) — **not blocking**. **Danger/error color alignment (BX-I9B2d, commit `ae5cfc8`):** Stitch v2.2 Error / Rose Red **`#E11D48`** in **`tokens.css`** only — **`--color-danger`** **`#e11d48`**; **`--color-danger-hover`** **`#be123c`**; **`--color-danger-fill`** **`#be123c`** (**not** **`#e11d48`** on fill — WCAG AA white labels on destructive buttons); **`--color-danger-fill-hover`** **`#9f1239`**; **`--color-danger-subtle`** **`rgba(225, 29, 72, 0.12)`**; **`--color-danger-border`** **`rgba(251, 113, 133, 0.35)`**; **`--color-error`** **`#fda4af`** (**not** **`#e11d48`** — readable error copy on dark glass); course accent rose tokens **unchanged**; **no** radius/canvas/shell/primary/cyan/AI/success/warning/chart/admin/editor/surface/text changes; **`components.css`** / **`layout.css`** / **`base.css`** untouched; **no** JSX/API/backend/package changes; stale red check passed; Supervisor Review **PASS**; Security / Trust Review **PASS**; merge QA **recommended** (danger border vs course rose border; delete buttons; **`.alert--error`**; **`.field__error`**; Trello failed rows) — **not blocking**. **Primary/cyan color alignment (BX-I9B2b, commit `b4d7b93`):** Stitch v2.2 primary blue + cyan/data tokens in **`tokens.css`** only — **`--color-primary`** **`#3B82F6`**; **`--color-primary-hover`** **`#2563EB`**; **`--color-primary-subtle`** / **`--color-primary-border`** re-based to **rgb(59, 130, 246)**; **`--color-primary-fill`** **`#2563EB`** / **`--color-primary-fill-hover`** **`#1D4ED8`** (**not** **`#3B82F6`** — filled primary buttons preserve WCAG AA with white text); **`--color-focus-ring`** remains **`var(--color-primary)`**; **`--shadow-focus`** / **`--shadow-focus-subtle`** / **`--shadow-focus-field`** / **`--glow-primary`** re-based to **rgb(59, 130, 246)**; **`--color-data-accent`** **`#06B6D4`**; **`--color-chart-series-1`** **`#06B6D4`**; **`--color-chart-series-2`** **`#3B82F6`**; **`--color-chart-fill`** **`rgba(6, 182, 212, 0.5)`**; **no** **`--radius-*`** changes; **no** canvas/shell (B2a) changes; **no** AI/violet/danger/error/success/warning/surface/text/editor/course-accent/admin/**`--glow-ai`** changes; **`components.css`** / **`layout.css`** / **`base.css`** untouched; **no** JSX/API/backend/package changes; indirect cascade via existing **`var(--color-primary*)`** / **`var(--color-data-accent)`** / chart vars **expected**; Supervisor Review **PASS**; Security / Trust Review **PASS**; authenticated visual smoke **recommended before merge** (especially primary links on glass/cards, dashboard hero cyan eyebrow, shell/nav/field focus rings) — **not blocking**. **Courses / course workspace presentation (BX-I6C, commit `6a1e6ad`):** Polished **subject shelf** on **`/courses`** (`courses-shelf--deck`, semantic **`ul > li > article`**, **`source-card--course-shelf`**, glass create form, empty-state wrapper); **subject workspace** on **`/courses/:id`** (subject pill using existing course accent, settings as secondary band, materials as primary glass zone, **`document-shelf--deck`** / **`source-card--document-shelf`**, honest local material count subtitle from already-loaded **`materials.length` only** — hidden during loading/error/empty; not health/progress/coverage/priority/AI); stronger tasks framing and danger-zone separation; **narrow responsive browser viewport ~375px** verified (no horizontal overflow); **no** backend/API/database/package/auth/routes/services changes; **no** `tokens.css`, dashboard, AppShell, or material cockpit changes; **no** chart libraries, sidebar, new packages, or fake course health/priority/urgency/status/AI classification; course accents remain supplementary visual chrome only; cards show titles/metadata only — **no** full material body on list/detail cards.

**Tasks / global task workspace presentation (B4-A, commit `4ae80ee`):** Polished **`/tasks`** page body only — task command surface / command band (`task-workspace__command-band--deck`), improved page hierarchy, filter toolbar visual framing, create/list/empty/error/loading wrappers, scoped empty/error/list visual treatment; semantic **`ul.task-workspace__list > li.task-workspace__list-item > TaskCard` article or FormCard section**; status filters use native **`<button type="button">`** with **`aria-pressed`** on the real DOM button (**B4-A-F1** — fixes prior issue where **`Button.jsx`** did not forward **`aria-pressed`**); selected status filter **not** color-only; **“Task command”** is approved UI framing only — **not** an AI/automation claim; **`task.priority`** remains API-backed only; **narrow responsive browser viewport ~375px** scoped CSS support; **frontend presentation-only** in **`GlobalTasksSection.jsx`**, **`TasksPage.jsx`**, **`layout.css`** only; **no** backend/API/database/package/auth/routes/services changes; **no** `tokens.css` or **`components.css`** changes; **no** task CRUD/filter/validation/**Focus** navigation/**`refreshStats`** behavior changes; **no** AppShell, dashboard, courses, course detail, material cockpit, flashcards, Trello, admin, or focus page changes; **no** fake AI/health/urgency/status/priority semantics or new task data exposure.

**Flashcards / global flashcard library presentation (B4-B, commit `f91415d`):** Polished **`/flashcards`** page body only — flashcards command surface / command band (`flashcards-workspace__command-band--deck`), improved page hierarchy, filters/create/study/manage visual framing, page-level loading/error wrappers, scoped loading/error/empty/filter-empty/action-error wrappers, manage list readability (`overflow-wrap` on truncated questions); filter group **`role="group"`** + **`aria-label="Filter saved flashcards"`** on native labeled course/material **`<select>`** elements; **“Flashcard library”** / **“Filter, study, and manage saved cards”** are factual UI framing only — **not** AI/automation claims; manage list still shows **truncated question only** — **no** answers newly exposed in manage list; full answers remain in existing study reveal (**`FlashcardStudy`**) and edit flows only; **`actionError`** moved outside **`FormCard`** into **`flashcards-workspace__action-error`** — presentation-only, still **`ErrorMessage`** / **`role="alert"`**; delete confirmation and destructive action clarity **unchanged**; **narrow responsive browser viewport ~375px** scoped CSS in **`layout.css`** only — **not** phone/native UI; **frontend presentation-only** in **`GlobalFlashcardsSection.jsx`**, **`FlashcardsPage.jsx`**, **`layout.css`** only; **no** backend/API/database/package/auth/routes/services changes; **no** `FlashcardStudy.jsx`, **`DbFlashcardsSection.jsx`**, **`components.css`**, or **`tokens.css`** changes; **no** flashcard business logic / generated plan logic / create/edit/delete/study/reveal behavior changes; **no** AppShell, dashboard, courses, course detail, tasks, material cockpit, Trello, admin, or focus page changes; **no** fake AI/mastery/progress/health/priority/urgency/status semantics; **no** token/session/user/flashcard logging.

**Trello / integration workspace presentation (B4-C, commit `cf50729`):** Polished **`/trello`** page body only — Trello integration command surface / command band (`trello-workspace__command-band--deck`); improved step framing for credentials, board/list picker, task selector, sync submit, and results; page-level loading/error visual wrappers; results zone visual framing; scoped **`layout.css`** polish; factual integration copy only (manual sync creates Trello cards in the selected list — **not** automatic or AI-driven); API key and token remain **`type="password"`** with **`autoComplete="off"`**; **no** reveal/show-token; credentials not displayed, logged, or stored in browser storage; **`clearCredentials`** and **`clearCredentialsAfterSync`** unchanged; trust notes honest and unchanged; **B4-C-F1** removed courses-level **Try again** (behavior deviation) — courses error remains **`ErrorMessage`**-only inside **`trello-workspace__page-error`**; task-load **Try again** unchanged; **narrow responsive browser viewport ~375px** scoped CSS — **not** phone/native UI; **frontend presentation-only** in seven approved files only; **no** backend/API/database/package/auth/routes/services, **`trello.service.js`**, validation/utils, **`components.css`**, **`tokens.css`**, AppShell, dashboard, courses, tasks, flashcards, material, admin, or focus changes; **no** credential lifecycle, sync payload, board/list/task loading, selection, result rendering, or sync behavior changes; **no** fake AI/smart-sync/health/progress/quality/priority/urgency semantics.

**Admin / platform aggregate workspace presentation (B4-D, commit `905ee4d`):** Polished **`/admin`** page body only — **`admin-workspace`** root class; admin command/read surface / command band (`admin-workspace__command-band--deck`); improved aggregate stat-band visual hierarchy; page-level loading/error/API-forbidden visual wrappers; admin trust note / disclaimer framing (`role="note"` — aggregate counts only, **no** emails/content/individual records); forbidden-card visual polish via scoped CSS; **Backend status** band title (renamed from **System health**) — still uses **`stats.systemHealth.backend`** through existing **`formatBackendHealth`** only (**no** fake security/risk/health score or AI monitoring); **“Platform control”** is approved read-only UI framing only — **not** user/role management (Security / Trust Review **approved with notes**; optional future copy softening to **“Platform overview”** is non-blocking); frontend **`user?.role`** / **`AdminRoute`** / **`App.jsx`** route tree / **`AppShell`** Admin nav unchanged — frontend role check remains UX-only; backend **`GET /api/admin/stats`** remains the real security boundary; **no** token/session/user/admin payload logging; **`loadStats`**, refresh, **Try again**, **FORBIDDEN**, **AUTH_REQUIRED**, and **SEC-6A3-1** behavior unchanged; **narrow responsive browser viewport ~375px** scoped CSS — **not** phone/native UI; **frontend presentation-only** in **`AdminDashboardPage.jsx`**, **`layout.css`** only; **no** **`AdminRoute.jsx`**, **`App.jsx`**, **`AppShell`**, **`admin.service.js`**, backend/API/database/package/auth/route/role-check changes; **no** dashboard/courses/tasks/flashcards/Trello/material/focus/**`components.css`**/**`tokens.css`** changes; **no** users/roles/logs/charts/destructive actions/new permissions.

**Focus / session cockpit presentation (B4-E, commit `7f4bf6b`):** Polished **`/focus/:taskId`** page body only — **`focus-workspace`** session deck / command band (`focus-workspace__command-band--deck`); improved task context framing; improved timer panel / action area / loading/error/done visual wrappers; factual session cockpit copy only (**“Session cockpit”**, **“Focus session”**, **25-minute** timer copy accurate because **`DEFAULT_DURATION_MINUTES = 25`** — future duration changes should update copy); **removed noisy `aria-live` from active timer panel**; static timer **`aria-label`** from **`session.durationMinutes`** only — **no** per-second live countdown announcements; error/done **`aria-live="polite"`** retained; **Complete session** and **Mark task as complete** remain explicit and user-controlled; done summary still uses backend **`completedSession.durationMinutes`**; **`taskTitle`** remains plain React text only; **no** token/session/user/focus payload logging; **no** `dangerouslySetInnerHTML`, `innerHTML`, `eval`, markdown renderer, or external assets; **narrow responsive browser viewport ~375px** scoped CSS — **not** phone/native UI; **frontend presentation-only** in **`FocusPage.jsx`** (JSX return / markup only — logic above return unchanged), **`layout.css`** only; **no** **`focus.service.js`**, **`TaskCard.jsx`**, **`App.jsx`**, **`AppShell`**, backend/API/database/package/auth/route changes; **no** timer/session behavior changes (auto-start, countdown math, **`beginFocusStart`** / **`focusStartRequests`**, phase machine, **`completeFocusSession`**, checkbox, **`refreshStats`**, error handling, navigation state/back links, **AUTH_REQUIRED**); **no** pause/reset/duration picker/history/charts/streaks/fake scores/AI focus coach/smart timer; **no** dashboard/courses/tasks/flashcards/Trello/admin/material/**`components.css`**/**`tokens.css`** changes.

**Shared state primitives presentation (B4-F1, commit `ea8a899`):** **CSS-only** visual polish in **`frontend/src/styles/components.css`** only — shared **`.loading`**, **`.empty-state`**, **`.alert`** / **`.alert--error`**, and **`.protected-loading`** primitives aligned with dark graphite / glass B4 command-surface language; glass panel styling for loading; glass deck card for empty states; error alert with danger tokens and left accent (errors remain visible — **not** softened into success/healthy); auth-gate **`protected-loading`** gradient + centered nested loading cap; **narrow responsive browser viewport ~375px** safety via **`min-width: 0`**, **`overflow-wrap: anywhere`**, and scoped padding at **`max-width: 640px`**; spinner and **`prefers-reduced-motion`** loading behavior preserved; **no** **`LoadingState.jsx`**, **`EmptyState.jsx`**, or **`ErrorMessage.jsx`** changes — **`LoadingState`** still **`role="status"`** / **`aria-live="polite"`**; **`ErrorMessage`** still **`role="alert"`**; **`EmptyState`** props and primary CTA behavior unchanged; **no** copy changes; **no** new/removed **Try again** buttons; **no** new empty/loading/error states; **TrelloTaskSelector** EmptyState prop bug **intentionally not fixed**; **no** fake AI/health/priority/urgency/status semantics; **no** route/page/service/API/auth/data-fetching/error-mapping changes; **no** **`layout.css`** or **`tokens.css`** changes; B4-A–E page-scoped wrappers preserved (B4-A/B empty-state scoped overrides still apply); material AI processing lane may look slightly nested (glass **`.loading`** inside existing **`.ai-panel__loading--active`**) — cosmetic/non-blocking per reviews; **no** token/session/user/content/error-payload logging; **no** unsafe rendering.

**Secondary in-page state surfaces (B4-F3A, commit `596e869`):** **CSS-only** in **`frontend/src/styles/layout.css`** only — secondary in-page state surface polish on **`/courses/:id`** and **`/study-materials/:materialId`**; **CourseDetail** materials area loading/error/empty CSS; **CourseTasksSection** filter-empty via **`.section__meta`** (filter-scoped — **not** deletion/data-loss semantics); material saved flashcards library loading/error/empty; material cockpit **plan-panel__error** / **plan-history__error** block spacing, wrapping, and safe **Try again** spacing; **narrow responsive browser viewport ~375px** safety (**`min-width: 0`**, **`overflow-wrap: anywhere`**, scoped padding at **`max-width: 640px`**) — **not** phone/native UI; StudyOps AI remains a **browser-based WEB platform / SaaS dashboard / AI study cockpit** only. **No** JSX at B4-F3A; **`components.css`**, **`tokens.css`**, shared UI component, page, route, service, API, auth, data-fetching, error-mapping, retry, handler, or copy changes; **no** new/removed **Try again** buttons; **no** role or **`aria-live`** changes; **B4-F2** route-level **`__page-loading`** / **`__page-error`** / **`__not-found`** wrappers **unchanged**; material AI processing lane (**`ai-panel__loading`**, “Processing with AI…”) **not touched at B4-F3A**; **GeneratedPlanHistory** preview **`aria-live="polite"`** **not touched at B4-F3A**; **TrelloTaskSelector** EmptyState prop bug **deferred at B4-F3A** — **fixed in B4-F3C1** (commit **`d0393d7`**); **B4-F3B** wrapper hooks shipped separately (commit **`ee5357f`**); Dashboard in-body empty, **TrelloBoardListPicker** / **TrelloSyncResults** **not touched at B4-F3A**. **Accessibility preserved:** **`LoadingState`** **`role="status"`** / **`aria-live="polite"`**; **`ErrorMessage`** **`role="alert"`**; **Try again** / EmptyState CTA behavior unchanged. **Trust / security:** empty states remain informational; filter-empty remains filter-scoped; **no** raw API payloads or sensitive details newly exposed; **no** full material/plan/flashcard content newly shown in state panels; **no** token/session/user/content/error-payload logging; **no** unsafe rendering. **Minor non-blocking note:** **flashcard-library__error** and **plan-panel__error** have neutral outer card shells — inner **`.alert--error`** remains clearly error-oriented. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed; authenticated visual QA **not live-tested**.

**State surface wrapper hooks (B4-F3B, commit `ee5357f`):** **Frontend presentation-only** in four approved files — **`CourseDetail.jsx`**, **`CourseTasksSection.jsx`**, **`DbFlashcardsSection.jsx`**, **`layout.css`**; adds explicit wrapper **`className`** hooks around existing secondary in-page loading/error/empty state surfaces; migrates brittle **B4-F3A** direct-child/generic **`layout.css`** selectors to stable wrapper selectors; wrappers added: **`course-workspace__materials-loading`**, **`course-workspace__materials-error`**, **`course-workspace__materials-error-actions`**, **`course-workspace__materials-empty`**, **`course-workspace__tasks-filter-empty`**, **`flashcard-library__loading`**; **no** copy, behavior, API/service/auth/route, **`LoadingState.jsx`**, **`ErrorMessage.jsx`**, **`EmptyState.jsx`**, **`tokens.css`**, **`components.css`**, role, or **`aria-live`** changes; **no** StudyMaterialDetail generate-lane, **GeneratedPlanHistory** preview, Trello, Dashboard, or global tasks/global flashcards changes; wrapper divs do **not** add roles or **`aria-live`** — **no** nested live regions; error visibility via **`ErrorMessage`** **`role="alert"`**; loading via **`LoadingState`** **`role="status"`** / **`aria-live="polite"`**; Try again and EmptyState CTA handlers unchanged; visual output intended unchanged; **narrow responsive browser viewport ~375px** behavior preserved — **not** phone/native UI; StudyOps AI remains a **browser-based WEB platform / SaaS dashboard / AI study cockpit** only; **no** sensitive data newly rendered; **no** console logging; **no** unsafe rendering; all wrapper classNames are fixed strings. **B4-F3A** / **B4-F3B** / **B4-F3C** (**B4-F3C1** **`d0393d7`**, **B4-F3C2** **`d1a3c69`**, **B4-F3C3** **`ab28307`**) are all **complete**. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes** — **no** Critical or Important issues; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed; authenticated browser smoke **not live-tested** — future recommended QA: **`/courses/:id`** materials loading/error/empty, Try again and EmptyState CTA, Pending/Completed filter-empty, **`/study-materials/:id`** flashcards library loading/error/empty, **narrow responsive browser viewport ~375px**, keyboard focus on Try again / EmptyState CTA, console clean.

**GeneratedPlanHistory preview aria-live cleanup (B4-F3C3, commit `ab28307`):** **Frontend-only** one-file accessibility cleanup in **`frontend/src/components/materials/GeneratedPlanHistorySection.jsx`** only — removed **`aria-live="polite"`** from preview panel wrapper **`plan-history__preview plan-history__preview-panel`**; preview loading **`<p>`** now has **`role="status"`** and **`aria-live="polite"`** for **Loading preview…** only; **`previewError`** still **`ErrorMessage`** / **`role="alert"`** unchanged; preview success (truncated snippet + aggregate meta/counts via **`getPlanSummarySnippet`** / **`formatPlanPreviewMeta`**) unchanged and **not** inside any **`aria-live`** region — success **not** live-announced; **no** full generated plan body, task list, flashcards, key topics list, raw JSON, or raw API payloads rendered; full plan may remain in React state after **`getGeneratedPlanById`** (unchanged — not newly DOM-exposed); **no** **`LoadingState.jsx`**, **`ErrorMessage.jsx`**, **`EmptyState.jsx`**, CSS (**`layout.css`**, **`components.css`**, **`tokens.css`**), services/API/auth/backend/package, **`StudyMaterialDetail.jsx`**, **`TrelloTaskSelector.jsx`**, or **`generated-plan-history-preview.js`** changes; **no** copy, button, route, or behavior changes; visual UI unchanged; **no** console logging or unsafe rendering; StudyOps AI remains **browser-based WEB platform / SaaS dashboard / AI study cockpit** only; **375px** checks are **narrow responsive browser viewport** checks — **not** phone/native design. Supervisor Review **approved**; Security / Trust Review **approved** — **no** Critical or Important issues; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed; manual smoke **limited** — authenticated generated-plan-history preview QA not live-tested — future recommended QA: study material with history → Preview inactive version → **Loading preview…** visible (polite announcement if testable) → snippet + meta only on success → **`previewError`** if safely triggerable → keyboard on Preview / Make active / Delete → **narrow responsive browser viewport ~375px** → console clean.

**AI processing lane aria-live cleanup (B4-F3C2, commit `d1a3c69`):** **Frontend-only** one-file accessibility cleanup in **`frontend/src/pages/StudyMaterialDetail.jsx`** only — removed duplicate **`aria-live="polite"`** from outer **`<div className="ai-panel__loading ai-panel__loading--active">`** wrapper in the **`{generating && (…)}`** block; **`LoadingState`** remains the single polite live region for **Processing with AI…** (**`role="status"`**, **`aria-live="polite"`**); preserved **`<LoadingState message="Processing with AI…" />`**, visible processing panel, and disabled generate button label **Processing with AI…**; **no** **`LoadingState.jsx`**, **`ErrorMessage.jsx`**, or **`EmptyState.jsx`** changes; **no** CSS (**`layout.css`**, **`components.css`**, **`tokens.css`**) changes; **no** generate/**`handleGenerate`**/**`generateDisabled`**/**`generateError`**/plan/history/flashcards/material-editor behavior or copy changes; **no** backend/API/database/package/auth/route guard/service changes; **no** **`TrelloTaskSelector`** or **`GeneratedPlanHistorySection`** changes; **no** B4-F3C3 **GeneratedPlanHistory** preview **`aria-live`** work; processing state remains visible — **not** hidden or softened into success; **`generateError`** still **`ErrorMessage`** / **`role="alert"`**; **no** material content, generated plan payloads, tokens, sessions, users, or raw error-payload logging; **no** console logging; **no** unsafe rendering; StudyOps AI remains **browser-based WEB platform / SaaS dashboard / AI study cockpit** only; **375px** checks are **narrow responsive browser viewport** checks — **not** phone/native design. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes** — **no** Critical or Important issues; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed; authenticated generate-flow smoke **not live-tested** — future recommended QA: open study material, **Generate study plan**, disabled button + panel **Processing with AI…**, single polite live region via **`LoadingState`** if testable, success path, **`generateError`** if safely triggerable, **narrow responsive browser viewport ~375px**, keyboard focus, console clean.

**TrelloTaskSelector empty-state bug fix (B4-F3C1, commit `d0393d7`):** **Frontend-only** isolated bug fix in **`frontend/src/components/trello/TrelloTaskSelector.jsx`** only — fixed zero-tasks empty state that used invalid **`EmptyState`** with unsupported **`message`** prop (could render blank heading/description/button and undefined **`onAction`** click risk); removed unused **`EmptyState`** import; replaced with plain informational **`<p className="trello-picker__empty trello-picker__status" role="status">`** preserving exact copy: **No study tasks yet. Create tasks on a course or the All study tasks page.**; **no** CSS changes (**`layout.css`**, **`components.css`**, **`tokens.css`** unchanged); **no** **`EmptyState.jsx`**, **`LoadingState.jsx`**, or **`ErrorMessage.jsx`** changes; **no** **`TrelloSyncSection.jsx`**, Trello services, sync payload, credentials/board/list flow, or Trello sync behavior changes; **no** backend/API/database/package/auth/route guard changes; **no** new CTAs, navigation, or **Try again** button changes; **no** B4-F3C2 AI processing lane **`aria-live`** cleanup; **no** B4-F3C3 **GeneratedPlanHistorySection** preview **`aria-live`** / error semantics work; material AI processing lane **not touched**; **GeneratedPlanHistorySection** **not touched**; task selection, Select all, Clear, 50-task limit, **`overLimit`** **`role="alert"`**, disabled behavior, checkbox labels, and task title/meta rendering when tasks exist **unchanged**; zero-tasks state exposes **no** Trello credentials, payloads, tokens, session/user data, or task titles; **no** console logging; **no** unsafe rendering; StudyOps AI remains a **browser-based WEB platform / SaaS dashboard / AI study cockpit** only; **375px** checks are **narrow responsive browser viewport** checks — **not** phone/native design. Supervisor Review **approved**; Security / Trust Review **approved** — **no** Critical, Important, or Minor issues; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed; authenticated browser smoke **not live-tested** — future recommended QA: **`/trello`** zero tasks (correct empty message, no blank heading/button, console clean), **`/trello`** with tasks (select/deselect, Select all, Clear, over-limit alert), **narrow responsive browser viewport ~375px**, keyboard focus, console clean.

**Route state surface framing (B4-F2, commit `ee50b8e`):** **Frontend presentation-only** in five approved files only — **`DashboardStub.jsx`**, **`CoursesList.jsx`**, **`CourseDetail.jsx`**, **`StudyMaterialDetail.jsx`**, **`layout.css`**; route-level loading/error/not-found visual framing for **`/dashboard`** page loading/error wrappers, **`/courses`** page loading/error wrappers, **`CourseDetail`** early-return loading/error/not-found wrappers, and **`StudyMaterialDetail`** early-return loading/error/not-found wrappers; scoped **`layout.css`** route-state styles; neutral not-found decks for course/material missing-resource states (**Course not found** / **Study material not found** — **no** admin/forbidden/permission-denied wording); wrapped error action rows for existing **Try again** buttons only (**loadStats**, **loadCourses**, **loadCourse**, **loadMaterial** — unchanged handlers and visibility); **narrow responsive browser viewport ~375px** scoped CSS safety (**not** phone/native UI); StudyOps AI remains a **browser-based WEB platform / SaaS dashboard / AI study cockpit** only. **Accessibility preserved:** **`LoadingState`** still **`role="status"`** / **`aria-live="polite"`**; **`ErrorMessage`** still **`role="alert"`**; **no** duplicate **`aria-live`** wrappers; back links remain React Router **`Link`** elements; **`h1`** remains in not-found states. **Minor non-blocking note:** course/material not-found and page-error decks share neutral glass deck framing with primary top accent; error states still use **`ErrorMessage`** danger styling inside the deck. **No** backend/API/database/package/auth/route guard/**`AppShell`**/service/data-fetching/error-mapping/retry behavior changes; **no** copy changes; **no** new/removed **Try again** buttons; **no** **`components.css`** or **`tokens.css`** changes; **no** shared UI component changes; **no** B4-F1 primitive CSS changes; **B4-F3** secondary empty surfaces **not started**; **TrelloTaskSelector** EmptyState prop bug **intentionally not fixed**; **CourseDetail** nested materials states **not touched**; **StudyMaterialDetail** success cockpit / AI / plan / flashcards sections **not touched**; material AI processing lane **not touched**; Tasks/Flashcards/Trello/Admin/Focus pages **not touched**; **no** route transitions, sidebar/drawer/bottom-tabs/phone-style UI; **no** token/session/user/content/error-payload logging; **no** unsafe rendering. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed; authenticated visual QA **limited** (unauthenticated fake course UUID redirected to sign-in).

**Not live:** chart UI, sidebar shell, course accents on material detail, **final Stitch-level desktop SaaS layout** (per-page **BX-I7** desktop density **complete** through **BX-I7F** at **`25988dc`** — UI improved but **not** claimed as final Stitch-perfect product; optional **BX-I7D Tier 2** materials | tasks side-by-side — **not implemented**; **BX-I7E3 Tier B** Trello task-list grid — **not implemented**), remaining **B4** global styling rollout beyond **B4-A** + **B4-B** + **B4-C** + **B4-D** + **B4-E** + **B4-F1** + **B4-F2** + **B4-F3A** + **B4-F3B** + **B4-F3C1** + **B4-F3C2** + **B4-F3C3** (**B4-F3A** / **B4-F3B** / **B4-F3C** all **complete**) — require separate planning and explicit approval.

**Branch (as of DOCS-FLASHCARD-REVIEW-A3):** post-**FLASHCARD-REVIEW-A3** frontend review-state filter on saved DB flashcards (global **`/flashcards`** + material detail); prior **DOCS-FLASHCARD-REVIEW-A2** / **FLASHCARD-REVIEW-A2** Known/Unknown review; prior **DOCS-FLASHCARD-REVIEW-A1** / **FLASHCARD-REVIEW-A1** backend (**`POST /api/flashcards/:flashcardId/review`**, migration **013**); prior **DOCS-TASK-MATERIAL-DETAIL-A3**; prior **TASK-MATERIAL-DETAIL-A3**; prior **DOCS-BX-I10A6-QA** Trello failed-row live verification (**Pass with notes**, review only, **no** implementation commit); prior **BX-I10A5** targeted glass/elevation pass (commit **`38aa561`**); **BX-I10A5-SMOKE** logged-in visual smoke **Pass with notes**; prior **BX-I10A4** (commit **`7e5e61f`**); prior **BX-I10A3** (commit **`cb54ec5`**); prior **BX-I10A2** (commit **`b90108e`**); prior **BX-I10A1** (commit **`e62c1b0`**); prior **BX-I9C** / **BX-I9C-Auth** final visual QA **passed with notes**; **BX-I9B2d** (commit **`ae5cfc8`**); **BX-I9B2c**–**BX-I9B1** **complete**; **BX-I8E**–**BX-I7** **complete**

---

## Current phase

| Phase | Status | Notes |
|-------|--------|-------|
| **FLASHCARD-REVIEW-A3** | **Complete** | Frontend-only — client-side review-state filter on **saved DB flashcards**; **`filterFlashcardsByReviewState`** in **`flashcard-filters.js`**; material detail **`DbFlashcardsSection`** + global **`/flashcards`** **`GlobalFlashcardsSection`**; filter options **All**, **New + Learning**, **New**, **Learning**, **Known** by **`mastery`**; affects study deck and manage list; composes with course/material filters on **`/flashcards`**; **no** backend **`?mastery=`**; **no** URL-persisted filters; **no** sorting by **`lastReviewedAt`**; **FLASHCARD-REVIEW-A2** Known/Unknown unchanged (**no** refetch; **no** auto-advance); plan JSON **`GeneratedPlanSection`** **no** review filter/API; **not** full SRS; **no** **`next_review_at`**; **no** SM-2/Anki; **no** dashboard due cards; **no** review history table; Supervisor Review **approved with notes**; Security Review **not required**; **`npm run lint`**, **`npm test`**, **`npm run build`** passed; main CI **green** |
| **DOCS-FLASHCARD-REVIEW-A3** | **Complete** | Documentation-only — records **FLASHCARD-REVIEW-A3** shipped behavior; **no** repo code changes |
| **FLASHCARD-REVIEW-A2** | **Complete** | Frontend-only — Known/Unknown on **saved DB flashcards** after answer reveal; material detail **`DbFlashcardsSection`** + global **`/flashcards`** **`GlobalFlashcardsSection`**; **`POST /api/flashcards/:flashcardId/review`** `{ outcome: "known" \| "unknown" }` via **`reviewFlashcard`**; local merge by id (**no** refetch on review); user stays on same card; plan JSON **`GeneratedPlanSection`** **no** review buttons/API; **not** full SRS; **no** **`next_review_at`**; **no** SM-2/Anki; **no** dashboard due cards; review-state filter shipped in **FLASHCARD-REVIEW-A3**; Supervisor Review **approved with notes**; Security Review **not required**; **`npm run lint`**, **`npm test`** (**337** pass), **`npm run build`** passed; main CI **green** |
| **DOCS-FLASHCARD-REVIEW-A2** | **Complete** | Documentation-only — records **FLASHCARD-REVIEW-A2** shipped behavior; **no** repo code changes |
| **FLASHCARD-REVIEW-A1** | **Complete** | Backend/database only — migration **`013_flashcard_review_state.sql`** **applied successfully**; aggregate columns **`mastery`**, **`last_reviewed_at`**, **`review_count`**, **`known_count`**, **`unknown_count`** on **`public.flashcards`**; **`POST /api/flashcards/:flashcardId/review`** body `{ outcome: "known" \| "unknown" }`; review state **not** writable via PATCH/create; **no** **`next_review_at`**; **no** review history table; frontend Known/Unknown shipped in **FLASHCARD-REVIEW-A2**; review-state filter shipped in **FLASHCARD-REVIEW-A3**; Supervisor Review **approved with notes**; Security Review **PASS WITH NOTES**; backend **`npm run lint`** + **`npm test`** passed; main CI **green** |
| **DOCS-FLASHCARD-REVIEW-A1** | **Complete** | Documentation-only — records **FLASHCARD-REVIEW-A1** shipped behavior; **no** repo code changes |
| **FLASHCARD-REVIEW-A4** | **Not started** | Full SRS scheduling; backend **`?mastery=`** query params; **`next_review_at`**; SM-2/Anki; dashboard due cards; review history table; URL-persisted filters; sorting by **`lastReviewedAt`** — **deferred**; requires separate planning + explicit approval |
| **TASK-MATERIAL-DETAIL-A3** | **Complete** | Frontend-only — read-only **Related study tasks** band on **`/study-materials/:materialId`** (between cockpit and **Saved flashcards**); **`listCourseTasks`** + client-side material filter; pending/completed/total counts; up to **5** compact preview rows; loading/empty/error + **Try again**/populated; **`Link`** to **`/courses/:courseId`**; refreshes after plan task import; distinguishes saved DB tasks from AI plan JSON; **no** **`TaskCard`**, edit, complete, delete, or Focus; **no** backend **`materialId`** query params, DB/migration/API, or URL params; Security Review **not required**; **`npm test`**, lint, build passed; manual smoke **passed**; follow-up manual smoke **passed** |
| **DOCS-TASK-MATERIAL-DETAIL-A3** | **Complete** | Documentation-only — records **TASK-MATERIAL-DETAIL-A3** shipped behavior; **no** repo code changes |
| **TASK-MATERIAL-FILTERS-A2** | **Complete** | Frontend-only — study material filters on **`/courses/:id`** (always, using course-page materials) and **`/tasks`** (only when a specific course is selected, via **`listMaterials`**); filters already-loaded tasks by **`task.materialId`** in memory; composes with status/course filters; global course change resets material filter; unknown/stale filter id shows all tasks; **no** backend **`materialId`** query params, DB/migration/API, or URL params; Security Review **not required**; **`npm test`**, lint, build passed; manual smoke **Pass with notes** |
| **DOCS-TASK-MATERIAL-FILTERS-A2** | **Complete** | Documentation-only — records **TASK-MATERIAL-FILTERS-A2** shipped behavior; **no** repo code changes |
| **TASK-MATERIAL-LINKS-A1** | **Complete** | Frontend-only — **`TaskCard`** material **`Link`** to **`/study-materials/:materialId`** when `task.materialId` set; link text from `materialLabel` or fallback **View study material**; no material line when `materialId` absent; **no** backend/API/database/migration changes; Security Review **not required**; **`npm test`**, lint, build passed; GitHub CI on **main** green; material label resolution improved on global **`/tasks`** when a course is selected (**TASK-MATERIAL-FILTERS-A2**) |
| **DOCS-TASK-MATERIAL-LINKS-A1** | **Complete** | Documentation-only — records **TASK-MATERIAL-LINKS-A1** shipped behavior; **no** repo code changes |
| **DOCS-WEB-ONLY** | **Complete** | Product scope clarification — StudyOps is **web-only**; 375px = responsive browser viewport; agent terminology; Stitch = web UI reference only. **Documentation only.** |
| **DOCS-A1** | **Complete** | Source-of-truth cleanup (CURRENT_STATE, AGENT_MEMORY, PRD clarifications). |
| **DOCS-A2** | **Complete** | Documentation reality-check audit only — no file changes. |
| **DOCS-A3** | **Complete** | Closed DOCS-A2 gaps in **`IMPLEMENTATION_STATUS`** + phase pointers. **Documentation only.** |
| **BX-I7A** | **Complete** | Desktop WEB layout width audit — **documentation only**. Recorded desktop width/layout gap at prior **1120px** global cap; many routes still one-column/list/form stacks; Stitch/**`DESIGN.md`** imply wider desktop SaaS cockpit — global cap addressed in **BX-I7B**, per-page density **not** solved. **No** implementation in **BX-I7A**. |
| **BX-I7B** | **Complete** | Global desktop cockpit shell widening — **CSS-only** (commit **`00d3255`**); **`frontend/src/styles/tokens.css`** + **`frontend/src/styles/layout.css`** only; **`--content-max-cockpit`** and **`--content-max-shell`** **1120px → 1280px**; **`--content-max-form`**, **`--content-max-workspace`**, **`--content-max-reading`** unchanged; **`AppShell`** + **`page--cockpit`** aligned; desktop-only **`.page`** horizontal padding at **`min-width: 1280px`**; **narrow responsive browser viewport ~375px** preserved; **no** JSX/components/pages/services/backend/API/auth/routes; **no** per-page grid redesign, fake metrics, sidebar, or mobile/native UI; **foundation only** — does **not** fully solve Stitch/design gap; authenticated visual smoke **limited/not fully completed**. Supervisor Review **approved with notes**. **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **BX-I7C** | **Complete** | Dashboard desktop grid expansion — **CSS-only** (commit **`583922d`**); **`frontend/src/styles/layout.css`** only; at **`min-width: 1280px`**: **PageHeader** full width; **dashboard-hero** + **dashboard-study-pulse** side-by-side (hero full width when study pulse absent); **dashboard-courses** / **dashboard-secondary** full width below; **dashboard-course-list** 2-column grid; loading/error unaffected (`:has(.dashboard-hero)`); **narrow responsive browser viewport ~375px** stacked; **no** **`DashboardStub.jsx`**, API/backend/database/service/context/recommendation/copy changes; **no** fake metrics, charts, sidebar, mobile/native UI; improves Dashboard desktop density — **not** final Stitch-level UI; authenticated dashboard visual smoke **recommended before merge** if not yet done. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **BX-I7D Tier 1** | **Complete** | Courses / course detail desktop shelf expansion — **CSS-only** (commit **`52c68ab`**); **`frontend/src/styles/layout.css`** only; at **`min-width: 1280px`**: **`/courses`** course shelf **3-column** grid; **`/courses/:id`** populated document/material shelf **2-column** grid; loading/error/empty unaffected; tasks below materials unchanged; **375px** stacked; **no** **`CoursesList.jsx`**, **`CourseDetail.jsx`**, component, API/backend/database/service/context/recommendation/copy changes; **no** fake metrics, charts, sidebar, mobile/native UI; **Tier 2** side-by-side workspace **not implemented**; improves Courses/Course Detail density — **not** final Stitch-level UI; authenticated visual smoke **recommended before merge** if not yet done. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **BX-I7D Tier 2** | **Not started** | **Optional / proposed:** materials | tasks side-by-side **`.course-workspace`** full-page grid — requires separate planning + explicit approval — **not automatic**. |
| **BX-I7E1** | **Complete** | Tasks desktop panels — **CSS-only** (commit **`d0db43e`**); **`frontend/src/styles/layout.css`** only; at **`min-width: 1280px`**: **`/tasks`** horizontal command-controls band, filters use available width, toolbar aligns right when space allows, populated task list **2-column** grid; create FormCard full-width; inline edit intended full width via **`:has(.task-workspace__edit-card)`**; loading/error/empty/filter-empty unaffected; **375px** stacked; **no** **`GlobalTasksSection.jsx`**, **`TasksPage.jsx`**, **`TaskCard.jsx`**, API/service/context/copy changes; **no** fake metrics, charts, sidebar, mobile/native UI; improves Tasks desktop density — **not** final Stitch-level UI; inline edit / console smoke **recommended before merge** if not yet done. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **BX-I7E2** | **Complete** | Flashcards desktop panels — **CSS-only** (commit **`b18304c`**); **`frontend/src/styles/layout.css`** only; at **`min-width: 1280px`**: **`/flashcards`** horizontal command-controls band, filters use available width, toolbar aligns right when space allows, populated library **study | manage** **2-column** layout (`:has(.flashcards-workspace__study-zone)`), manage list **2-column** grid when populated, create/inline edit full-width (`:has(form)`); intro/loading/error/empty/filter-empty/status/create-cta full-width; **`actionError`** outside library unaffected; manage list truncated questions only — **no** answer exposure in manage list; **375px** stacked; **no** **`GlobalFlashcardsSection.jsx`**, **`FlashcardsPage.jsx`**, **`FlashcardStudy.jsx`**, API/service/context/copy changes; **no** fake metrics, charts, sidebar, mobile/native UI; **no** mastery/AI/memory/health score; improves Flashcards desktop density — **not** final Stitch-level UI; loading/error/**`actionError`** / console / keyboard tab smoke **recommended before merge** if not yet done (optional). Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **BX-I7E3 Tier A** | **Complete** | Trello desktop setup panels — **CSS-only** (commit **`cba6dde`**); **`frontend/src/styles/layout.css`** only; at **`min-width: 1280px`**: **`/trello`** Step 1 credentials + Step 2 destination side-by-side; Step 3 tasks, messages, Step 4 sync full-width; results below command band; DOM order unchanged; **375px** stacked; **no** Trello JSX/components/services/API/backend changes; **Tier B not implemented** (no task-list grid, results grid, wizard/results side-by-side, scroll-height override); improves Trello desktop density — **not** final Stitch-level UI; authenticated visual smoke **recommended before merge** if not yet done (optional). Supervisor Review **approved**; Security / Trust Review **approved**; **`npm run lint`**, **`npm test`**, **`npm run build`** passed. |
| **BX-I7E3 Tier B** | **Not started** | **Proposed:** Trello desktop Tier B (task-list grid, results grid, wizard/results side-by-side, scroll-height override) — requires separate planning + explicit approval — **not automatic**. |
| **BX-I7E4** | **Complete** | Admin desktop stats panels — **CSS-only** (commit **`467ccd9`**); **`frontend/src/styles/layout.css`** only; at **`min-width: 1280px`**: **`/admin`** stats deck **2-column** grid; Platform overview, Trello today, Backend status full-width; Tasks|Focus and Learning|Trello stats rows side-by-side; inner stat grids unchanged; loading/error/forbidden/**`AdminRoute`** unaffected; **375px** stacked; **no** JSX/API/backend/services/auth/copy changes; **no** fake metrics/charts/health scores/new KPIs; improves Admin desktop density — **not** final Stitch-level UI; authenticated visual smoke **recommended before merge** if not yet done (optional). Supervisor Review **approved**; Security / Trust Review **approved**; **`npm run lint`**, **`npm test`**, **`npm run build`** passed. |
| **BX-I7F** | **Complete** | Material cockpit desktop pass — **CSS-only** (commit **`25988dc`**); **`frontend/src/styles/layout.css`** only; at **`min-width: 1280px`** on **`/study-materials/:materialId`**: cockpit **Source | AI** **1.15fr | 0.85fr** (`:has(.material-workspace__cockpit)`); plan-history / plan-import action rows horizontal with wrap; material library **study | manage** **2-column** (`:has(.flashcard-library__study)`); manage list **2-column**; create/edit full-width (`:has(form)`); loading/error/not-found unaffected; **375px** stacked; **`/flashcards`** unaffected; **no** JSX/API/services/backend/auth/AI behavior/logging/content-exposure changes; plan history single-column / preview unchanged; improves material cockpit desktop density — **not** final Stitch-level UI; authenticated visual smoke **recommended before merge** if not yet done (optional). Security / Trust Review **PASS** (prior gate). |
| **DOCS-BX-I7F-HOUSEKEEPING** | **Complete** | **BX-I7 closeout** documentation alignment after commit **`25988dc`** — **documentation only**. |
| **BX-I8B** | **Complete** | AI command surfaces polish — **CSS-only** (commit **`bda2645`**); **`frontend/src/styles/components.css`** + **`frontend/src/styles/layout.css`** only; on **`/study-materials/:materialId`**: material-detail AI command column uses stronger violet/glass treatment (generate panel, loading lane, plan output, history, import bands, cockpit AI shell); **Generate** CTA remains electric blue / primary; **375px** stacked; **BX-I7F** material cockpit desktop layout preserved; **no** `tokens.css`, JSX, API, services, backend, auth, routes, behavior, copy, logging, or content-exposure changes; **no** fake metrics, charts, scores, sidebar, or mobile-native UI; improves AI command surface polish — **not** final Stitch-level UI; authenticated visual smoke **recommended before merge** if not yet done (optional — does **not** block commit). Security / Trust Review **PASS** (prior gate). **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **DOCS-BX-I8B-HOUSEKEEPING** | **Complete** | Documentation alignment after commit **`bda2645`** — **documentation only**. |
| **BX-I8C** | **Complete** | Auth + PageHeader intro chrome — **CSS-only** (commit **`8008dc1`**); **`frontend/src/styles/components.css`** + **`frontend/src/styles/layout.css`** only; auth screens: glass/command-center **`.page--auth .form-card`**; intro **`PageHeader`**: glass intro band + top gradient rule (**.page-header--intro** only; non-intro **`.page-header`** unchanged); **`.auth-brand`** polish on **`/`** and **`/register`**; course accent header preserved; **375px** stacked; **BX-I7** / **BX-I8B** layouts preserved; **no** `tokens.css`, **`base.css`**, JSX, API, backend, auth, routes, services, behavior, copy, logging, or content-exposure changes; **no** fake metrics, charts, scores, sidebar, or mobile-native UI; focus-visible clipping smoke on intro header actions **recommended but not blocking**; Security / Trust Review **PASS** (prior gate). **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **DOCS-BX-I8C-HOUSEKEEPING** | **Complete** | Documentation alignment after commit **`8008dc1`** — **documentation only**. |
| **BX-I8D** | **Complete** | Motion micro-pass — **CSS-only** (commit **`51cdc77`**); **`frontend/src/styles/components.css`** + **`frontend/src/styles/base.css`** only; processing opacity pulse on **`.ai-panel__loading--active .loading`** only during real AI generate; one-shot **`plan-fade-in`** on **`.plan-panel__status--success`**, **`.plan-history__preview-panel`**, **`.focus-done`**; **`prefers-reduced-motion`** disables pulse/entrances/press transforms; **375px** stacked; **BX-I7** / **BX-I8B** / **BX-I8C** layouts preserved; **no** JSX/JS/API/backend/routes/tokens/behavior/copy/**`aria-live`** changes; **no** route transitions, **`App.jsx`**, **`useLocation`**, fake idle AI thinking, fake metrics/charts, or idle decorative infinite animation; manual smoke **recommended before merge**, **not blocking**; Security / Trust Review **PASS** (prior gate). **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **DOCS-BX-I8D-HOUSEKEEPING** | **Complete** | Documentation alignment after commit **`51cdc77`** — **documentation only**. |
| **BX-I8E** | **Complete** | Shared controls / card surface alignment — **CSS-only** (commit **`52b7b78`**); **`frontend/src/styles/components.css`** only — **`.form-card`** baseline glass-border + subtle raised gradient; **`.source-card`** baseline glass-border only; **`.link-btn`** parity with **`.btn`** variants (**`--primary`** / **`--danger`** unused in JSX); **`.alert--success`** parity with error alerts; **`layout.css`** / **`tokens.css`** / **`base.css`** untouched; **no** JSX/API/backend/routes/services/behavior/copy/logging changes; optional scope **not implemented** (no **`dashboard-empty-cta__link`**, empty/status unification, command-band/hero/AI cockpit/auth PageHeader/BX-I7 grid); Visual Style Guide v2.2 alignment check **passed**; token/radius/font/AI-gradient gaps **deferred**; **no** fake metrics/charts/scores/sidebar/mobile-native UI; manual smoke **recommended before merge**, **not blocking**; Security / Trust Review **PASS** (prior gate). **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **DOCS-BX-I8E-HOUSEKEEPING** | **Complete** | Documentation alignment after commit **`52b7b78`** — **documentation only**. |
| **DOCS-I8F** | **Complete** | Stitch visual style guide v2.2 reference artifact — **`docs/design/studyops_ai_visual_style_guide_v2_2.md`** recorded; **`DESIGN.md`** + status docs aligned; **documentation only**; **BX-I8E** alignment check **passed**; **BX-I8F** visual smoke audit **passed with notes**; deferred optional follow-ups listed — **not automatic**. |
| **BX-I9B1** | **Complete** | Radius token alignment (Stitch Round Eight) — **CSS-only** (commit **`9ec2917`**); **`frontend/src/styles/tokens.css`** + **`frontend/src/styles/components.css`** only; **`--radius-sm`**: **8px**; **`--radius-md`**: **12px**; **`--radius-lg`** / **`--radius-xl`** unchanged; **`.btn`** / **`.link-btn`** use **`--radius-md`**; fields remain **`--radius-sm`**; **no** color/shadow/spacing/typography token changes; **`layout.css`** / **`base.css`** untouched; **no** JSX/API/backend/routes/services changes; **no** AI Generate gradient; **no** flashcard-study polish; indirect token cascade to pills/cards **expected**; authenticated visual smoke **recommended before merge**, **not blocking**; prior Security / Trust Review **PASS**. **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **DOCS-BX-I9B1-HOUSEKEEPING** | **Complete** | Documentation alignment after commit **`9ec2917`** — **documentation only**. |
| **BX-I9B2a** | **Complete** | Canvas/shell color token alignment (Stitch v2.2) — **CSS-only** (commit **`f92cbda`**); **`frontend/src/styles/tokens.css`** only; canvas/shell environment tokens only — **`--color-bg`** **`#0F172A`**; **`--color-bg-subtle`** harmonized; **`--color-bg-auth`** harmonized; **`--color-shell-bg`** toward **`#0B0F1A`** (translucency preserved); **`--color-shell-border`** slightly softened; **no** **`--radius-*`** changes; **no** primary/AI/cyan/danger/focus/glow/shadow/surface/text/editor/chart/course-accent token changes; **`components.css`** / **`layout.css`** / **`base.css`** untouched; **no** JSX/API/backend/package changes; **no** AI Generate gradient; **no** flashcard-study polish; **no** glass/elevation pass; indirect cascade via **`layout.css`** **`var(--color-bg*)`** / shell vars **expected**; Supervisor Review **PASS**; Security / Trust Review **PASS**; authenticated visual smoke **recommended before merge**, **not blocking**. **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **DOCS-BX-I9B2a-HOUSEKEEPING** | **Complete** | Documentation alignment after commit **`f92cbda`** — **documentation only**. |
| **BX-I9B2b** | **Complete** | Primary/cyan color token alignment (Stitch v2.2) — **CSS-only** (commit **`b4d7b93`**); **`frontend/src/styles/tokens.css`** only; primary blue family — **`--color-primary`** **`#3B82F6`**; **`--color-primary-hover`** **`#2563EB`**; subtle/border rgba **rgb(59, 130, 246)**; fill **`#2563EB`** / fill-hover **`#1D4ED8`** (**not** **`#3B82F6`** on fill — WCAG AA white labels); focus/glow/shadow-focus* re-based **rgb(59, 130, 246)**; cyan/data — **`--color-data-accent`** **`#06B6D4`**; chart **`--color-chart-series-1`** **`#06B6D4`**; **`--color-chart-series-2`** **`#3B82F6`**; **`--color-chart-fill`** **`rgba(6, 182, 212, 0.5)`**; **no** radius/canvas/shell/AI/violet/danger/surface/text/editor/course-accent/admin/**`--glow-ai`** changes; **`components.css`** / **`layout.css`** / **`base.css`** untouched; **no** JSX/API/backend/package changes; indirect cascade **expected**; Supervisor Review **PASS**; Security / Trust Review **PASS**; authenticated visual smoke **recommended before merge** (primary links on glass/cards, hero cyan eyebrow, focus rings) — **not blocking**. **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **DOCS-BX-I9B2b-HOUSEKEEPING** | **Complete** | Documentation alignment after commit **`b4d7b93`** — **documentation only**. |
| **BX-I9B2c** | **Complete** | AI/violet color token alignment (Stitch v2.2) — **CSS-only** (commit **`19d444e`**); **`tokens.css`** AI/violet family + **`--glow-ai`**; **`components.css`** (**20** hardcoded lavender → violet replacements); **`layout.css`** (**3** replacements); alphas preserved; **no** radius/canvas/shell/primary/cyan/danger/error changes; **no** JSX/API/backend/package changes; **Generate** CTA remains primary blue; stale lavender check passed; Supervisor Review **PASS**; Security / Trust Review **PASS**; authenticated visual smoke **recommended before merge** (material AI cockpit, dashboard hero violet tint, AppShell accent strip, auth brand gradient at **375px** / **≥1280px**) — **not blocking**. **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **DOCS-BX-I9B2c-HOUSEKEEPING** | **Complete** | Documentation alignment after commit **`19d444e`** — **documentation only**. |
| **BX-I9B2d** | **Complete** | Danger/error color token alignment (Stitch v2.2 Error / Rose Red **`#E11D48`**) — **CSS-only** (commit **`ae5cfc8`**); **`frontend/src/styles/tokens.css`** only — danger/error family (**`--color-danger`** **`#e11d48`**; **`--color-danger-fill`** **`#be123c`** **not** **`#e11d48`**; **`--color-error`** **`#fda4af`** **not** **`#e11d48`**); course accent rose tokens **unchanged**; **no** radius/canvas/shell/primary/cyan/AI/success/warning/chart/admin/editor/surface/text changes; **`components.css`** / **`layout.css`** / **`base.css`** untouched; **no** JSX/API/backend/package changes; stale red check passed; Supervisor Review **PASS**; Security / Trust Review **PASS**; merge QA **recommended** (danger border vs course rose border; delete buttons; **`.alert--error`**; **`.field__error`**; Trello failed rows) — **not blocking**. **`npm run lint`**, **`npm test`** (**228/228**), **`npm run build`** passed. |
| **DOCS-BX-I9B2d-HOUSEKEEPING** | **Complete** | Documentation alignment after commit **`ae5cfc8`** — **documentation only**. |
| **BX-I9C** | **Complete** | Final visual QA / smoke audit after **BX-I9B1** + **BX-I9B2a**–**BX-I9B2d** — **review only** (**no** repo file changes). **BX-I9C** initial pass: **Pass with notes** on auth routes **`/`**, **`/register`** at **~375px** / **1280px** / **1440px**; authenticated matrix **blocked** (no valid session on dev origin). **BX-I9C-Auth** completed authenticated matrix on **`http://localhost:5173`**: **`/dashboard`**, **`/courses`**, **`/courses/:id`**, **`/study-materials/:id`**, **`/tasks`**, **`/flashcards`**, **`/trello`** (failed sync rows **not exercised** at **BX-I9C-Auth** time — later covered in **BX-I10A6-QA**), **`/focus/:taskId`**, **`/admin`** student forbidden, plus auth routes. **Verdict:** **Pass with notes** — **no** critical or blocking important issues; **no** horizontal overflow on checked routes; radius/canvas/primary/cyan/AI/violet/danger/error checks **held**; material **Source \| AI** side-by-side at desktop, stacked at **375px**; **Generate** CTA remains primary blue; course accents distinct from danger/error; **no** console errors or sensitive logging observed. **Non-blocking:** Trello failed rows not live-tested at **BX-I9C-Auth** time; keyboard/focus automation limited. **Not** claimed final Stitch-perfect. |
| **BX-I9C-Auth** | **Complete** | Authenticated visual QA completion for **BX-I9C** — **review only** on Vite dev **`http://localhost:5173`**; temporary runtime test data only (**not** repo content): account **`bx-i9c-auth-20260603@example.test`**, course **BX-I9C QA Course**, material **BX-I9C QA Material**, task **BX-I9C QA Task**. |
| **DOCS-BX-I9C** | **Complete** | Documentation record of **BX-I9C** + **BX-I9C-Auth** visual QA outcomes — **documentation only**. |
| **BX-I10A1** | **Complete** | Flashcard study glass polish — **CSS-only** (commit **`e62c1b0`**); **`frontend/src/styles/components.css`** only; frosted glass **`.flashcard-study`** stage (centered max-width, gradient surface, glass border, backdrop blur); improved Q/A hierarchy and counter readability; **`flashcard-study--plan`** violet accent preserved; **FlashcardStudy** behavior unchanged (reveal, prev/next, counter, plain-text rendering); **Generate** CTA remains primary blue; **no** JSX/API/backend/package/token changes; Supervisor Review **Approve — safe to commit**; Security / Trust Review **Pass**; **`npm run lint`**, **`npm test`**, **`npm run build`** passed; logged-in smoke **Pass with notes** on **`/flashcards`** and **`/study-materials/:id`** at **~375px** / **1280px** — **no** horizontal overflow; material **Source \| AI** desktop layout held; **non-blocking:** **`flashcard-study--plan`** not live-verified (no generated plan with flashcards on QA material). |
| **DOCS-BX-I10A1** | **Complete** | Documentation record of **BX-I10A1** + **BX-I10A1-SMOKE** outcomes — **documentation only**. |
| **BX-I10A2** | **Complete** | Material-only AI Generate gradient — **CSS-only** (commit **`b90108e`**); **`frontend/src/styles/components.css`** only; scoped selector **`.page--material-detail .material-workspace__cockpit-ai .material-workspace__generate .ai-panel__actions .btn.btn--primary`** — restrained violet→blue gradient on **Generate study plan** only; global **`.btn--primary`** unchanged; **Save changes** and other primary CTAs (auth, dashboard, flashcards, Trello, tasks) remain standard blue; **no** JSX/API/backend/package/token/layout/base changes; handler, loading/disabled logic, API flow, and copy **unchanged**; Supervisor Review **Approve with notes**; Security / Trust Review **Pass with notes**; **`npm.cmd run lint`**, **`npm.cmd test`**, **`npm.cmd run build`** passed; **BX-I10A2-SMOKE** **Pass with notes** on **`/study-materials/:id`** (**~375px**, **1280px**), **`/dashboard`**, **`/`**, **`/register`**, **`/flashcards`**, **`/trello`** — **no** horizontal overflow; material **Source \| AI** desktop layout held; **no** sensitive console logs; **non-blocking:** Generate loading not live-triggered; plan history/import primary buttons not visible (no saved plan on QA material); expected **generated-plan** **404** handled as empty state. |
| **DOCS-BX-I10A2** | **Complete** | Documentation record of **BX-I10A2** + reviews + **BX-I10A2-SMOKE** outcomes — **documentation only**. |
| **BX-I10A3** | **Complete** | Filter-toolbar / command-surface glass unification — **CSS-only** (commit **`cb54ec5`**); **`frontend/src/styles/layout.css`** only; global **`.filter-toolbar`** glass/inset surface (gradient, **`--color-glass-border`**, **`elevation-1`**, **`backdrop-filter`**); **`.filter-toolbar--segmented .filter-toolbar__segment`** restrained inset glass; task/flashcard/course-detail filter surfaces inherit shared glass; quiet-glass filter-empty strips (**`.task-workspace__filter-empty`**, **`.flashcards-workspace__filter-empty`**, **`.course-workspace__tasks-filter-empty`**) — global **`.section__meta`** **not** broadly rewritten; tasks/flashcards **`.empty-state`** downgrades neutralized (global **`.empty-state`** applies); **`prefers-reduced-motion`** disables toolbar backdrop blur; **no** JSX/API/backend/package/**`components.css`**/**`tokens.css`**/**`base.css`** changes; filter/search/task behavior unchanged; Supervisor Review **Approve with notes**; Security / Trust Review **Pass with notes**; **`npm.cmd run lint`**, **`npm.cmd test`**, **`npm.cmd run build`** passed; **BX-I10A3-SMOKE** **Pass with notes** on **`/tasks`**, **`/flashcards`**, **`/courses/:id`** (**~375px**, **1280px**), **`/study-materials/:id`** spot check — **no** horizontal overflow; **BX-I10A1** flashcard study surface unchanged; **BX-I10A2** Generate gradient unchanged; material AI cockpit unchanged; **no** sensitive console logs observed; **non-blocking:** **`/flashcards`** filter-empty and true empty state not reachable with current QA seed; full browser console export limited by MCP. |
| **DOCS-BX-I10A3** | **Complete** | Documentation record of **BX-I10A3** + reviews + **BX-I10A3-SMOKE** outcomes — **documentation only**. |
| **BX-I10A4** | **Complete** | Course/document accent rail consistency — **CSS-only** (commit **`7e5e61f`**); **`frontend/src/styles/components.css`** + **`frontend/src/styles/layout.css`** only; **6px** course identity rails on subject shelf, course-shelf, dashboard course cards, and dashboard stat chips; course detail intro inset **4px**; materials primary section **4px** top rule; **3px** muted **`color-mix`** document-shelf left hint on course detail (full accent on hover); document/material cards remain secondary to course identity; **no** JSX/**`StudyMaterialDetail.jsx`**/**`tokens.css`**/**`base.css`**/backend/API/package changes; **BX-I10A1** flashcard study, **BX-I10A2** Generate gradient, **BX-I10A3** filter toolbar, material AI cockpit unchanged; Supervisor Review **Approve with notes**; Security / Trust Review **Pass with notes**; **`npm.cmd run lint`**, **`npm.cmd test`**, **`npm.cmd run build`** passed; **BX-I10A4-SMOKE** **Pass with notes** on **`/courses`**, **`/dashboard`**, **`/courses/:id`** (**~375px**, **1280px**), **`/study-materials/:id`**, **`/flashcards`** — **no** horizontal overflow; **non-blocking:** rose accent not in QA seed; document-shelf pointer hover not fully verified; MCP console export limited. |
| **DOCS-BX-I10A4** | **Complete** | Documentation record of **BX-I10A4** + reviews + **BX-I10A4-SMOKE** outcomes — **documentation only**. |
| **BX-I10A5** | **Complete** | Targeted glass/elevation pass — **CSS-only** (commit **`38aa561`**); **`frontend/src/styles/components.css`** + **`frontend/src/styles/layout.css`** only; softened navigable-card hover (**`--elevation-1`**, **`transform: none`**); quiet inline glass on tasks create/edit, flashcards library/create, Trello wizard/sync/results steps, course settings/create-material/materials empty; optional Trello picker row polish + Focus session outer panel softening; **`prefers-reduced-motion`** disables new glass **`backdrop-filter`**; **no** JSX/API/backend/package/**`tokens.css`**/**`base.css`** changes; **BX-I10A1** flashcard study, **BX-I10A2** Generate gradient, **BX-I10A3** filter toolbar, **BX-I10A4** accent rails, dashboard hero, material AI cockpit, material editor/library elevation **unchanged**; Supervisor Review **Pass with notes**; Security / Trust Review **Pass with notes**; **`npm.cmd run lint`**, **`npm.cmd test`**, **`npm.cmd run build`** passed; **BX-I10A5-SMOKE** **Pass with notes** on **`/dashboard`**, **`/courses`**, **`/courses/:id`**, **`/tasks`**, **`/flashcards`**, **`/trello`**, **`/focus/:taskId`**, **`/study-materials/:id`** at **~375px** / **1280px** — **narrow responsive browser viewport**, **not** native mobile; **no** horizontal overflow; **non-blocking:** materials empty dashed state not reachable (QA course had one material); real pointer hover not fully exercised by MCP; MCP console export limited. |
| **DOCS-BX-I10A5** | **Complete** | Documentation record of **BX-I10A5** + reviews + **BX-I10A5-SMOKE** outcomes — **documentation only**. |
| **BX-I10A6-QA** | **Complete** | Trello failed-row live verification — **review only** on Vite dev **`http://localhost:5173`**; route **`/trello`** at **~375px** / **1280px** (**narrow responsive browser viewport**, **not** native mobile). **Verdict:** **Pass with notes** — failed row triggered live (harmless fake API key/token; placeholder list ID via one-time runtime workaround; **no** real Trello credentials; **no** cards created); backend **HTTP 401** / **`TRELLO_AUTH`**; Step 5 **Sync results** readability, rose-vs-danger, and Trello regressions **passed**; results/failed row **no** horizontal overflow; credentials **`type="password"`**, cleared after sync; **no** credentials in DOM text; backend structured log only — **no** API key/token dumps. **Non-blocking:** list ID workaround (picker requires loaded board/list); top-nav horizontal scroll at **~375px** pre-existing shell behavior (unrelated to failed-row styling); full frontend console history not captured retroactively. **No** repo file changes; **no** fix phase required. |
| **DOCS-BX-I10A6-QA** | **Complete** | Documentation record of **BX-I10A6-QA** Trello failed-row live verification — **documentation only**. |
| **BX-I1** | **Complete** | Stitch visual direction in `DESIGN.md` v2.3 (commit **`6041932`**) — **docs only**; **reference** — production UI is **not** full Stitch-level yet (see **BX-I7A**). |
| **BX-I2** | **Complete** | Dark graphite / glass global token foundation — **CSS-only** (commit **`03ee9df`**); **`tokens.css`**, **`components.css`**, **`layout.css`** only. Supervisor Review **approved with notes**; Security Review **approved with limitation** (authenticated visual QA deferred — no local test account). |
| **BX-I3** | **Complete** | Dashboard decision layout — **frontend-only** (commit **`bdd6f2a`**); rule-based **“What should I study next?”** hero, study pulse / task progress bars, enhanced course workload rows, secondary **At a glance** stat bands; **`GET /api/dashboard/stats`** only; **14** recommendation unit tests (**219/219** total). Supervisor Review **approved with notes**; Security / Trust Review **approved with limitation** (authenticated dashboard manual smoke not fully completed — no approved local test account). |
| **BX-I4** | **Complete** | Deterministic course accents — **frontend-only** (commit **`ff65e21`**); seven approved files only; stable **`amber` | `rose` | `emerald`** mapping from existing course ID/name/title; **`CourseCard`** rail/pill, **`CourseDetail`** header, dashboard workload rows; enum-only accent keys — **no** raw user strings as class names; **no** logging, persistence, backend/API, DB, or package changes; accents are supplementary chrome only (**no** health score, priority, urgency, active/quiet state, or AI classification). **228/228** tests. Supervisor Review **approved with notes**; Security / Trust Review **approved** (authenticated visual QA not fully completed — no approved valid local test account). |
| **BX-I5** | **Complete** | Material cockpit visual polish — **frontend-only** (commit **`c2288d4`**); five approved files only on **`/study-materials/:materialId`**; improved Source \| AI hierarchy, darker source/editor well, source-type pill, AI command-column surfaces, plan/history/import/flashcards library polish, responsive breakpoints; **no** `tokens.css`, backend/API/database/package/auth/routes/services, behavior, or payload changes; **no** unsafe rendering (`dangerouslySetInnerHTML`, `innerHTML`, markdown renderer — material and plan content remain plain React text). **228/228** tests. Supervisor Review **approved with notes**; Security / Trust Review **PASS** (authenticated manual smoke / visual QA **not completed** — no approved valid local test account). |
| **BX-I6B** | **Complete** | Dashboard visual upgrade / AI Study Command Center presentation — **frontend-only** (commit **`cceb4e0`**); three approved files only on **`/dashboard`**: **`DashboardStub.jsx`**, **`layout.css`**, **`components.css`**; flagship recommendation hero, glass/depth/glow (existing tokens), Study pulse cockpit band (Pending/Completed/Total from stats), richer course workload deck, **At a glance** tertiary; **narrow responsive browser viewport ~375px** — no mid-word stat label breaks, no horizontal overflow; reduced-motion for decorative effects; **no** `tokens.css`, backend/API/database/package/auth/routes/services, **`dashboard-recommendation.js`**, **`DashboardContext`**, **`dashboard.service.js`**, chart libraries, sidebar, weekly focus chart, or new packages; **no** fake AI confidence/urgency/priority/health/analytics. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Supervisor re-check **approved with notes**; Security / Trust Review **approved with notes**; manual authenticated dashboard smoke **passed**. |
| **BX-I6C** | **Complete** | Courses / course-detail visual alignment — **frontend-only** (commit **`6a1e6ad`**); six approved files only: **`CourseCard.jsx`**, **`MaterialCard.jsx`**, **`CoursesList.jsx`**, **`CourseDetail.jsx`**, **`layout.css`**, **`components.css`**; **`/courses`** subject shelf + create form glass panel + empty wrapper; **`/courses/:id`** subject workspace hierarchy, materials primary zone, document shelf deck, tasks/danger framing; semantic **`ul > li > article`**; honest material count subtitle (**`materials.length`**, no new API); **narrow responsive browser viewport ~375px** — no horizontal overflow; **no** backend/API/database/package/auth/routes/services, `tokens.css`, dashboard, AppShell, or material cockpit changes; **no** chart libraries, sidebar, new packages, or fake health/priority/urgency/status/AI metrics. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; manual authenticated smoke **passed** (empty state not smoke-tested). |
| **BX-I6D** | **Complete** | Global shell / top navigation / visual chrome polish — **CSS-only** (commit **`9252ba9`**); **`frontend/src/styles/layout.css`** only; glass WEB dashboard shell bar, accent strip, brand/nav/logout hover and **`:focus-visible`**; active route styling **not** color-only; logout visible and labeled; **narrow responsive browser viewport ~375px** — WEB top-nav horizontal scroll row (**not** phone UI); **no** bottom tabs, drawer, hamburger-first layout; **`prefers-reduced-motion`** for shell transitions; **no** `AppShell.jsx`, `App.jsx`, auth, route guards, route fade, page transitions, `useLocation`, `key={location.pathname}`, tokens/dashboard/course/material page changes, backend/API/database/package changes, or misleading AI/priority/urgency/status copy. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; authenticated **375px** shell spot-check **passed with notes** (Flashcards focus near nav edge — minor, non-blocking). |
| **B4-A** | **Complete** | Tasks page body polish — **frontend presentation-only** (commit **`4ae80ee`**); three approved files only: **`GlobalTasksSection.jsx`**, **`TasksPage.jsx`**, **`layout.css`** on **`/tasks`** only; task command surface / command band, filter toolbar framing, create/list/empty/error/loading wrappers, semantic **`ul > li`** task list; native status filter buttons with **`aria-pressed`** on real DOM (**B4-A-F1**); selected filter **not** color-only; **narrow responsive browser viewport ~375px** scoped CSS; **no** backend/API/database/package/auth/routes/services, `tokens.css`, **`components.css`**, task CRUD/filter/validation/**Focus**/**`refreshStats`** changes, or other route changes; **no** fake AI/health/urgency/status/priority semantics. **228/228** tests; lint + build passed. Supervisor Review **PASS**; Security / Trust Review **approved with notes**; **B4-A-F1** focused re-check **approved**; authenticated manual smoke **limited** — static review + automated checks passed. |
| **B4-B** | **Complete** | Flashcards page body polish — **frontend presentation-only** (commit **`f91415d`**); three approved files only: **`GlobalFlashcardsSection.jsx`**, **`FlashcardsPage.jsx`**, **`layout.css`** on **`/flashcards`** only; flashcards command surface / command band, filters/create/study/manage framing, scoped loading/error/empty/filter-empty/action-error wrappers, manage list readability; filter **`role="group"`** + **`aria-label="Filter saved flashcards"`**; manage list truncated question only — answers not newly exposed; **`actionError`** presentation-only move still **`ErrorMessage`** / **`role="alert"`**; **narrow responsive browser viewport ~375px** scoped CSS; **no** backend/API/database/package/auth/routes/services, `tokens.css`, **`components.css`**, **`FlashcardStudy.jsx`**, **`DbFlashcardsSection.jsx`**, flashcard CRUD/filter/study/reveal/**`refreshStats`** changes, or other route changes; **no** fake AI/mastery/progress/health/priority/urgency/status semantics. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; authenticated manual smoke **partial** — populated library, filters, study/reveal, 375px passed; create/edit/delete persistence and filter-empty/global-empty not exercised. |
| **B4-C** | **Complete** | Trello page body polish — **frontend presentation-only** (commit **`cf50729`**); seven approved files only on **`/trello`**: **`TrelloSyncPage.jsx`**, **`TrelloSyncSection.jsx`**, **`TrelloSyncForm.jsx`**, **`TrelloBoardListPicker.jsx`**, **`TrelloTaskSelector.jsx`**, **`TrelloSyncResults.jsx`**, **`layout.css`**; Trello integration command surface / command band, step framing, page loading/error wrappers, results zone framing; **B4-C-F1** removed courses-level **Try again** — courses error **`ErrorMessage`**-only; credentials remain masked; trust copy honest; **narrow responsive browser viewport ~375px** scoped CSS; **no** backend/API/database/package/auth/routes/services, **`trello.service.js`**, validation/utils, `tokens.css`, **`components.css`**, credential lifecycle, sync payload, board/list/task loading, selection, result rendering, or sync behavior changes; **no** fake AI/smart-sync/health/progress/quality/priority/urgency semantics. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; **B4-C-F1** Supervisor re-check **approved**; Security / Trust Review **approved with notes**; authenticated manual smoke **partial** — `/trello` load, masked credentials, task selector, 375px passed; no safe Trello credentials — no real sync, board/list load, or post-sync results exercised live. |
| **B4-D** | **Complete** | Admin page body polish — **frontend presentation-only** (commit **`905ee4d`**); two approved files only on **`/admin`**: **`AdminDashboardPage.jsx`**, **`layout.css`**; **`admin-workspace`** root; admin command/read surface / command band; improved aggregate stat-band hierarchy; page loading/error/forbidden wrappers; trust note (`role="note"`); forbidden-card polish; **Backend status** title rename only — same **`stats.systemHealth.backend`** / **`formatBackendHealth`**; aggregate-only — **no** PII/logs/payload display or logging; **no** **`AdminRoute.jsx`**, **`App.jsx`**, **`AppShell`**, **`admin.service.js`**, auth/route/**`user?.role`**/**`getAdminStats`**/**`loadStats`**/refresh/**FORBIDDEN**/**AUTH_REQUIRED**/SEC-6A3-1 changes; **no** dashboard/courses/tasks/flashcards/Trello/material/focus/**`components.css`**/**`tokens.css`** changes; **no** users/roles/logs/charts/new admin actions or fake security/risk/health/AI metrics; **narrow responsive browser viewport ~375px** scoped CSS — **not** phone/native UI. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; manual smoke **limited** — logged-out **`/admin` → `/`** confirmed; admin success/non-admin/API 403/375px success layout not live-tested (no sessions). |
| **B4-E** | **Complete** | Focus page body polish — **frontend presentation-only** (commit **`7f4bf6b`**); two approved files only on **`/focus/:taskId`**: **`FocusPage.jsx`**, **`layout.css`**; **`focus-workspace`** session deck / command band; improved task context, timer panel, action area, loading/error/done wrappers; factual session cockpit copy; **removed noisy `aria-live` from active timer panel**; static timer **`aria-label`** only; **no** timer/session logic changes (**`DEFAULT_DURATION_MINUTES = 25`**, auto-start, countdown, phase machine, complete/checkbox/**`refreshStats`**, errors, nav state, **AUTH_REQUIRED**); **no** **`focus.service.js`**, **`TaskCard`**, **`App.jsx`**, **`AppShell`**, backend/API/package/auth/route changes; **no** pause/reset/duration picker/history/charts/streaks/fake scores/AI coach; **no** dashboard/courses/tasks/flashcards/Trello/admin/material/**`components.css`**/**`tokens.css`** changes; **narrow responsive browser viewport ~375px** scoped CSS — **not** phone/native UI. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; manual smoke **limited** — no authenticated session/safe pending task; static review + automated checks passed. |
| **B4-F1** | **Complete** | Shared state primitives CSS polish — **CSS-only** (commit **`ea8a899`**); one approved file only: **`frontend/src/styles/components.css`**; polished **`.loading`**, **`.empty-state`**, **`.alert`** / **`.alert--error`**, **`.protected-loading`**; glass/dark UI alignment with B4 command surfaces; spinner + reduced-motion preserved; error visibility preserved (danger tokens + left accent); **narrow responsive browser viewport ~375px** wrap/padding rules; **no** **`LoadingState.jsx`**, **`EmptyState.jsx`**, **`ErrorMessage.jsx`**, **`layout.css`**, **`tokens.css`**, page, route, service, API, auth, or data-fetching changes; **no** copy changes; **no** new/removed **Try again** buttons; **TrelloTaskSelector** EmptyState prop bug **not fixed**; **`LoadingState`** **`role="status"`** / **`aria-live="polite"`** and **`ErrorMessage`** **`role="alert"`** unchanged; material AI nested-panel note cosmetic/non-blocking. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; manual smoke **limited** — full authenticated visual QA not live-tested. |
| **B4-F2** | **Complete** | Route state surface framing — **frontend presentation-only** (commit **`ee50b8e`**); five approved files only: **`DashboardStub.jsx`**, **`CoursesList.jsx`**, **`CourseDetail.jsx`**, **`StudyMaterialDetail.jsx`**, **`layout.css`**; route-level loading/error/not-found visual framing for Dashboard, Courses, CourseDetail early returns, StudyMaterialDetail early returns; neutral not-found decks; wrapped error action rows for existing **Try again** buttons; **narrow responsive browser viewport ~375px** scoped CSS; **no** backend/API/database/package/auth/route guard/service/data-fetching/error-mapping/retry/copy/**`components.css`**/**`tokens.css`**/shared UI component changes; **TrelloTaskSelector** EmptyState bug **not fixed**; nested materials / material success cockpit / AI lane **not touched**; **`LoadingState`** **`role="status"`** / **`aria-live="polite"`** and **`ErrorMessage`** **`role="alert"`** unchanged; not-found copy neutral — **no** forbidden/admin semantics; minor deck-framing note non-blocking. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; manual smoke **limited** — authenticated visual QA not live-tested. |
| **B4-F3A** | **Complete** | Secondary in-page state surfaces — **CSS-only** (commit **`596e869`**); one approved file only: **`frontend/src/styles/layout.css`**; **CourseDetail** materials loading/error/empty; course tasks filter-empty; material saved flashcards library loading/error/empty; material cockpit plan/history error block spacing/wrapping; **narrow responsive browser viewport ~375px** wrap/padding; **no** JSX at B4-F3A; **no** **`components.css`**/**`tokens.css`**/shared UI component/page/service/API/auth/data-fetching/error-mapping/retry/copy changes; **no** new/removed **Try again** buttons; **no** role/**`aria-live`** changes; **B4-F2** route wrappers unchanged; AI lane / history preview **`aria-live`** **not touched at B4-F3A**; **TrelloTaskSelector** EmptyState bug deferred here — **fixed in B4-F3C1**; **B4-F3B** wrapper hooks shipped separately (**`ee5357f`**); minor neutral outer shells on **flashcard-library__error** / **plan-panel__error** — inner **`.alert--error`** clear (non-blocking). **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; manual smoke **limited**. |
| **B4-F3B** | **Complete** | State surface wrapper hooks — **frontend presentation-only** (commit **`ee5357f`**); four approved files: **`CourseDetail.jsx`**, **`CourseTasksSection.jsx`**, **`DbFlashcardsSection.jsx`**, **`layout.css`**; explicit wrapper **`className`** hooks for secondary in-page loading/error/empty surfaces; **`layout.css`** selector migration from brittle **B4-F3A** direct-child/generic selectors to stable wrapper selectors; six wrappers: **`course-workspace__materials-loading`**, **`course-workspace__materials-error`**, **`course-workspace__materials-error-actions`**, **`course-workspace__materials-empty`**, **`course-workspace__tasks-filter-empty`**, **`flashcard-library__loading`**; **no** copy/behavior/API/service/auth/route/**`LoadingState.jsx`**/**`ErrorMessage.jsx`**/**`EmptyState.jsx`**/**`tokens.css`**/**`components.css`**/role/**`aria-live`** changes; **no** StudyMaterialDetail generate-lane, **GeneratedPlanHistory**, Trello, Dashboard, or global tasks/flashcards changes; wrapper divs **no** roles/**`aria-live`**; Try again / EmptyState CTA handlers unchanged; **narrow responsive browser viewport ~375px** preserved — **not** phone/native UI; **B4-F3A**/**B4-F3B**/**B4-F3C** all **complete**. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; manual smoke **limited** — authenticated QA recommended. |
| **B4-F3C1** | **Complete** | **TrelloTaskSelector** empty-state bug fix — **frontend-only** (commit **`d0393d7`**); one approved file only: **`frontend/src/components/trello/TrelloTaskSelector.jsx`**; removed invalid **`EmptyState`** **`message`** prop usage and unused import; zero-tasks state now plain **`<p role="status">`** with existing **`trello-picker__empty trello-picker__status`** classes and preserved empty copy; **no** CSS/**`EmptyState.jsx`**/**`LoadingState.jsx`**/**`ErrorMessage.jsx`**/**`TrelloSyncSection.jsx`**/services/API/auth/backend/package changes; **no** Trello sync behavior changes; task selection / Select all / Clear / 50-task limit / **`overLimit`** **`role="alert"`** unchanged; **no** credentials/payload/token/session/user/task-title exposure or logging; **no** B4-F3C2 AI lane or B4-F3C3 **GeneratedPlanHistory** work. **228/228** tests; lint + build passed. Supervisor Review **approved**; Security / Trust Review **approved**; manual smoke **limited** — authenticated **`/trello`** QA not live-tested. |
| **B4-F3C2** | **Complete** | AI processing lane **`aria-live`** cleanup — **frontend-only** (commit **`d1a3c69`**); one approved file only: **`frontend/src/pages/StudyMaterialDetail.jsx`**; removed duplicate **`aria-live="polite"`** from outer **`ai-panel__loading--active`** wrapper; **`LoadingState`** remains single polite live region (**`role="status"`**, **`aria-live="polite"`**) for **Processing with AI…**; visible processing panel and disabled button label unchanged; **no** **`LoadingState.jsx`**/**`ErrorMessage.jsx`**/**`EmptyState.jsx`**/CSS/services/API/auth/backend/package/generate/**`generateError`**/copy changes; **no** **`TrelloTaskSelector`** or **`GeneratedPlanHistorySection`** changes. **228/228** tests; lint + build passed. Supervisor Review **approved with notes**; Security / Trust Review **approved with notes**; manual smoke **limited** — authenticated generate-flow QA not live-tested. |
| **B4-F3C3** | **Complete** | **GeneratedPlanHistory** preview **`aria-live`** cleanup — **frontend-only** (commit **`ab28307`**); one approved file only: **`frontend/src/components/materials/GeneratedPlanHistorySection.jsx`**; removed wrapper **`aria-live`**; loading **`<p>`** has **`role="status"`** + **`aria-live="polite"`**; **`previewError`** still **`ErrorMessage`** / **`role="alert"`**; success snippet + meta only — **not** live-announced; **no** full plan body/tasks/flashcards/key topics/raw JSON exposure; **no** CSS/shared UI/services/API/auth/backend/package/**`StudyMaterialDetail`**/**`TrelloTaskSelector`**/preview-helper changes. **B4-F3C** sub-series (**B4-F3C1**, **B4-F3C2**, **B4-F3C3**) **complete**. **228/228** tests; lint + build passed. Supervisor Review **approved**; Security / Trust Review **approved**; manual smoke **limited** — authenticated preview QA not live-tested. |
| **B4** | **Partial** | **B4-A** **`/tasks`**, **B4-B** **`/flashcards`**, **B4-C** **`/trello`**, **B4-D** **`/admin`**, **B4-E** **`/focus/:taskId`**, **B4-F1** shared primitives polish, **B4-F2** route state framing, **B4-F3A** secondary in-page state surfaces, **B4-F3B** state surface wrapper hooks, **B4-F3C1** **TrelloTaskSelector** empty-state fix, **B4-F3C2** AI lane **`aria-live`** cleanup, and **B4-F3C3** **GeneratedPlanHistory** preview **`aria-live`** cleanup **complete** (commits **`4ae80ee`**, **`f91415d`**, **`cf50729`**, **`905ee4d`**, **`7f4bf6b`**, **`ea8a899`**, **`ee50b8e`**, **`596e869`**, **`ee5357f`**, **`d0393d7`**, **`d1a3c69`**, **`ab28307`**); **B4-F3A** / **B4-F3B** / **B4-F3C** all **complete**; remaining global styling rollout **not started** — requires separate planning and explicit approval. |
| **BX-I6** (umbrella) | **Partial** | **BX-I6B**, **BX-I6C**, **BX-I6D**, **B4-A**, **B4-B**, **B4-C**, **B4-D**, **B4-E**, **B4-F1**, **B4-F2**, **B4-F3A**, **B4-F3B**, **B4-F3C1**, **B4-F3C2**, and **B4-F3C3** presentation **complete**; **B4-F3A** / **B4-F3B** / **B4-F3C** all **complete**; sidebar shell, chart UI, chart APIs, and backend/API extension **not started** — separate gates each. |

---

## What is implemented

Full detail: **`docs/IMPLEMENTATION_STATUS.md`**.

| Area | Shipped |
|------|---------|
| **Auth** | Register, login, protected routes, profiles + admin role |
| **Courses & materials** | CRUD; material detail editor |
| **AI generate** | Material-scoped `POST …/generate` body `{}`; persisted plan; history (max 10/material); import tasks/flashcards with dedupe (**10B**) |
| **Tasks** | Course + global UI; status + **study material** filters (**TASK-MATERIAL-FILTERS-A2** — frontend-only, composes with status/course; global material filter only when a specific course is selected); edit pending; focus link; **`TaskCard`** material **`Link`** to **`/study-materials/:materialId`** when `materialId` set (**TASK-MATERIAL-LINKS-A1** — `materialLabel` or fallback **View study material**); material detail **Related study tasks** read-only band (**TASK-MATERIAL-DETAIL-A3** — client-side filter over **`listCourseTasks`**, up to 5 preview rows, link to course tasks) |
| **Flashcards** | Material + global UI; plan study (no review) + saved library CRUD + **Known/Unknown review on saved DB cards** (**FLASHCARD-REVIEW-A2**) |
| **Trello** | **A6:** board/list defaults persistence. **Primary UI sync (A5B):** when connected, boards/lists/sync use stored backend token — frontend sends `{}` / `{ listId, taskIds }` only; manual apiKey/token hidden. **Manual fallback:** collapsed advanced credentials when **disconnected** only. **A5C:** connected + manual `{ apiKey, token }` → **400**. **A2–A4-FRONTEND:** connect + callback. **A5A:** stored-token mode on boards/lists/sync API |
| **Focus** | 25-min timer MVP; start/complete |
| **Dashboard** | Aggregate stats; invalidation-only cross-page refresh (**5C.1**); rule-based **“What should I study next?”** decision hero, study pulse / task progress bars, course workload rows (**BX-I3**) |
| **Admin** | Aggregate stats only (`/admin`) — page body polish **B4-D**; no logs, user list, or role UI |
| **Presentation** | **8C-3D**, **12A-1**, **B1**–**B3**, **BX-I2** (dark glass tokens, shell/cockpit polish, cards/badges), **BX-I4** (deterministic course accents on list/detail/dashboard), **BX-I5** (material detail cockpit visual polish), **BX-I6B** (dashboard command-center visual upgrade), **BX-I6C** (courses / course-detail visual alignment), **BX-I6D** (global shell / top navigation chrome — **`layout.css`** only), **B4-A** (tasks page body / task command surface — **`GlobalTasksSection.jsx`**, **`TasksPage.jsx`**, **`layout.css`** only), **B4-B** (flashcards page body / flashcards command surface — **`GlobalFlashcardsSection.jsx`**, **`FlashcardsPage.jsx`**, **`layout.css`** only), **B4-C** (Trello page body / Trello integration command surface — **`TrelloSyncPage.jsx`**, **`components/trello/*`**, **`layout.css`** only), **B4-D** (admin page body / admin command read surface — **`AdminDashboardPage.jsx`**, **`layout.css`** only), **B4-E** (focus page body / session cockpit — **`FocusPage.jsx`**, **`layout.css`** only), **B4-F1** (shared state primitives — **`components.css`** only: **`.loading`**, **`.empty-state`**, **`.alert`** / **`.alert--error`**, **`.protected-loading`**), **B4-F2** (route state surface framing — **`DashboardStub.jsx`**, **`CoursesList.jsx`**, **`CourseDetail.jsx`**, **`StudyMaterialDetail.jsx`**, **`layout.css`**: Dashboard/Courses loading/error wrappers; CourseDetail/StudyMaterialDetail early-return loading/error/not-found wrappers), **B4-F3A** (secondary in-page state surfaces — **`layout.css`** only: CourseDetail materials loading/error/empty, course tasks filter-empty, material flashcards library states, material cockpit plan/history error blocks), **B4-F3B** (state surface wrapper hooks — **`CourseDetail.jsx`**, **`CourseTasksSection.jsx`**, **`DbFlashcardsSection.jsx`**, **`layout.css`**: explicit wrapper classNames + selector migration from brittle **B4-F3A** selectors), **B4-F3C1** (**TrelloTaskSelector** empty-state bug fix — **`TrelloTaskSelector.jsx`** only), **B4-F3C2** (AI processing lane **`aria-live`** cleanup — **`StudyMaterialDetail.jsx`** only), **B4-F3C3** (**GeneratedPlanHistory** preview **`aria-live`** cleanup — **`GeneratedPlanHistorySection.jsx`** only) — detail in **`IMPLEMENTATION_STATUS`** § UI polish |

---

## What is partial

| Item | Built | Missing / deferred |
|------|-------|-------------------|
| **Dashboard** | Decision hero, Study pulse cockpit band, course workload command deck, tertiary **At a glance** (**BX-I3** + **BX-I6B** presentation); desktop grid expansion at **≥1280px** — hero + study pulse side-by-side, 2-column course list (**BX-I7C**, commit **`583922d`**, **`layout.css`** only) | Chart UI, weekly focus buckets, course health / streak / deadline signals (BX-1 direction — docs only; **not** shipped); UI is **not** final Stitch-level |
| **Desktop layout (global)** | **`--content-max-cockpit`** / **`--content-max-shell`** **1280px** (**BX-I7B**, commit **`00d3255`**); dashboard desktop grid (**BX-I7C**, commit **`583922d`**); courses/course detail desktop shelves (**BX-I7D Tier 1**, commit **`52c68ab`**); **`/tasks`** desktop panels (**BX-I7E1**, commit **`d0db43e`**); **`/flashcards`** desktop panels (**BX-I7E2**, commit **`b18304c`**, **`layout.css`** only); **`/trello`** desktop setup panels (**BX-I7E3 Tier A**, commit **`cba6dde`**, **`layout.css`** only — credentials + destination 2-up at **≥1280px**); **`/admin`** desktop stats panels (**BX-I7E4**, commit **`467ccd9`**, **`layout.css`** only — stats deck 2-column at **≥1280px**); material cockpit desktop pass (**BX-I7F**, commit **`25988dc`**, **`layout.css`** only); dark glass tokens; cockpit class on hub routes; Source \| AI split at **≥1024px** on material detail | **BX-I7E3 Tier B** **not implemented**; optional **BX-I7D Tier 2** side-by-side workspace **not implemented**; UI is **not** final Stitch-level |
| **Courses** | List/detail/tasks/materials; deterministic per-course accent chrome (**BX-I4**); subject shelf + course workspace visual alignment (**BX-I6C**); desktop shelf grids at **≥1280px** — 3-column course list, 2-column document shelf (**BX-I7D Tier 1**, commit **`52c68ab`**) | Plan-coverage indicators (docs only); accent persistence in DB (not shipped); `/courses` empty state not manually smoke-tested; **BX-I7D Tier 2** side-by-side workspace **not implemented** |
| **Material detail** | Source \| AI cockpit + history + **Related study tasks** read-only band (**TASK-MATERIAL-DETAIL-A3**) + **BX-I2** dark tokens + **BX-I5** visual polish + **BX-I7F** desktop pass + **BX-I8B** AI command surfaces violet/glass polish | Course accents on material detail (**not shipped**); authenticated visual QA pending (no test account) |
| **Trello** | **A6:** board/list defaults persistence. **A5B:** connected-account sync primary UX on **`/trello`**; manual fallback when disconnected. **A5C (backend):** connected manual credentials blocked. **A4-FRONTEND:** Connect/Disconnect + OAuth callback. **A5A (backend):** stored-token mode on boards/lists/sync API. **A2–A4-STATE:** encrypted storage, backend routes, signed state | Card update/delete; force re-sync; **BX-I7E3 Tier B** **not implemented** |
| **Admin** | Platform aggregate counts; **`/admin`** page body presentation (**B4-D**); desktop stats deck grid at **≥1280px** (**BX-I7E4**, commit **`467ccd9`**, **`layout.css`** only) | Logs, user management, role management, Gemini metrics |
| **Design captures** | 14 Phase 2I PNGs + 3 Stitch refs | `15-processing-with-ai.png` pending; many PNGs predate **8C** / **11A-3** |

---

## What is not started

Requires **explicit human approval** (see `AGENTS.md`, `IMPLEMENTATION_STATUS` deferred list):

- **BX-I7D Tier 2** — optional materials | tasks side-by-side **`.course-workspace`** full-page grid. **Proposed**; **not started**; requires explicit approval (not automatic).
- **BX-I7E3 Tier B** — optional Trello desktop Tier B (2-column task list, results grid, wizard/results side-by-side, scroll-height override). **Proposed**; **not started**; requires explicit approval (not automatic).
- **B4** (remaining) — remaining global styling rollout beyond **B4-A** + **B4-B** + **B4-C** + **B4-D** + **B4-E** + **B4-F1** + **B4-F2** + **B4-F3A** + **B4-F3B** + **B4-F3C1** + **B4-F3C2** + **B4-F3C3** (requires separate planning and explicit approval each; **B4-F3A** / **B4-F3B** / **B4-F3C** all **complete** — commits **`596e869`**, **`ee5357f`**, **`d0393d7`**, **`d1a3c69`**, **`ab28307`**; other **B4** commits **`4ae80ee`**, **`f91415d`**, **`cf50729`**, **`905ee4d`**, **`7f4bf6b`**, **`ea8a899`**, **`ee50b8e`**)
- **BX-I6** (remaining) — sidebar shell, chart UI, chart APIs, backend/API extension **in code** (requires separate planning and explicit approval each)
- Sidebar shell migration (Stitch sidebar is reference-only)
- Course-level paste-generate (`POST /api/courses/:courseId/generate`)
- PDF upload/parsing; Trello card update/delete; force re-sync; payments; **full SRS** / spaced repetition (**FLASHCARD-REVIEW-A1** + **A2** + **A3** aggregate review + Known/Unknown + client-side review-state filter on saved DB cards only — **not** SM-2/Anki); backend **`GET /api/flashcards?mastery=`**; **`next_review_at`**; dashboard due cards; review history table; URL-persisted flashcard filters; sorting by **`lastReviewedAt`** (**FLASHCARD-REVIEW-A4** and related — not shipped); **production deployment** (cloud/hosted — local dev + CI only today)
- Dashboard polling / WebSockets / cross-tab sync
- Backend **`GET /api/tasks?materialId=`** / **`GET /api/courses/:id/tasks?materialId=`**; URL-persisted task filters; filter for tasks without a material link (frontend-only material filter shipped in **TASK-MATERIAL-FILTERS-A2**; material detail related tasks band shipped in **TASK-MATERIAL-DETAIL-A3**)
- `api_logs` / admin logs UI

---

## Current design direction

**Presentation authority:** `DESIGN.md` (v2.3) — UI/UX only; does **not** change product scope or APIs.

**Live values:** `frontend/src/styles/tokens.css` — authoritative for colors, spacing, shadows in production (dark graphite / glass after **BX-I2**).

**Approved direction (documented, not fully built):**

1. **Phase A / BX-1** — Modern AI study command center; decision-first dashboard; honest chart rules; course accents; strong Source | AI cockpit.
2. **BX-S / BX-I1** — Stitch-selected **dark graphite / glass** reference (`docs/design/STITCH_SELECTED_REFERENCE.md`, `docs/design/STITCH_VISUAL_STYLE_GUIDE.md`, `stitch-*.png`). **Reference only** — do not copy Stitch HTML/React into `frontend/src`. **BX-I2**–**BX-I6** + **B4** + **BX-I7B** shipped tokens, polish, and global shell widening — **not** the final Stitch-level desktop SaaS layout pass (per-page grid/density — **BX-I7F** proposed; **BX-I7E4** **complete** at **`467ccd9`**; **BX-I7E3 Tier A** **complete** at **`cba6dde`**).
3. **BX-I7A / BX-I7B / BX-I7C–F** — **BX-I7A** (audit, complete) documented the prior **1120px** global cap gap. **BX-I7B** (complete, commit **`00d3255`**) widened **`--content-max-cockpit`** / **`--content-max-shell`** to **1280px** in **`tokens.css`** + **`layout.css`** only — **foundation only**. **BX-I7C** (complete, commit **`583922d`**) expanded **dashboard** desktop grid in **`layout.css`** only — improves Dashboard density at **≥1280px**; does **not** fully solve Stitch/design gap. **BX-I7D Tier 1** (complete, commit **`52c68ab`**) expanded **courses/course detail** desktop shelves in **`layout.css`** only — **`/courses`** 3-column shelf + **`/courses/:id`** 2-column document shelf at **≥1280px**; **Tier 2** side-by-side workspace **not implemented**; does **not** fully solve Stitch/design gap. **BX-I7E1** (complete, commit **`d0db43e`**) expanded **`/tasks`** desktop panels in **`layout.css`** only — horizontal command-controls band + **2-column** populated task list at **≥1280px**; does **not** fully solve Stitch/design gap. **BX-I7E2** (complete, commit **`b18304c`**) expanded **`/flashcards`** desktop panels in **`layout.css`** only — horizontal command-controls band + **study | manage** **2-column** populated library + **2-column** manage list at **≥1280px**; manage list still does not expose answers; does **not** fully solve Stitch/design gap. **BX-I7E3 Tier A** (complete, commit **`cba6dde`**) expanded **`/trello`** desktop setup panels in **`layout.css`** only — Step 1 credentials + Step 2 destination side-by-side at **≥1280px**; tasks/messages/sync full-width; results below command band; **Tier B not implemented**; does **not** fully solve Stitch/design gap. **BX-I7E4** (complete, commit **`467ccd9`**) expanded **`/admin`** desktop stats deck in **`layout.css`** only — stats deck **2-column** grid at **≥1280px**; Platform overview, Trello today, Backend status full-width; Tasks|Focus and Learning|Trello stats rows side-by-side; inner stat grids unchanged; **no** JSX/API/backend/services/auth/copy changes; does **not** fully solve Stitch/design gap. **BX-I7F** (proposed, **not started**) = material cockpit final desktop pass — **not automatic**; optional **BX-I7D Tier 2** and **BX-I7E3 Tier B** require separate planning + explicit approval.

**MVP navigation:** Top **`AppShell`** bar remains approved; Stitch sidebar mockups are visual reference only.

**Editing `DESIGN.md` does not approve implementation.** Separate gates required for `tokens.css`, frontend/CSS, chart libraries, fonts/CDN, and API extensions.

---

## Next approved / suspended phases

| Phase | Gate |
|-------|------|
| **BX-I2** | **Complete** (commit **`03ee9df`**) — follow-up: authenticated visual QA when a test account exists |
| **BX-I3** | **Complete** (commit **`bdd6f2a`**) — follow-up: authenticated dashboard visual QA when a test account exists (dashboard with data, empty dashboard, hero CTAs, refresh stats, study pulse / progress bars, **narrow responsive browser viewport ~375px**, console check for no token/secret/material-content logs) |
| **BX-I4** | **Complete** (commit **`ff65e21`**) — follow-up: authenticated visual QA when a test account exists (**`/courses`** list, **`/courses/:id`** detail header, **`/dashboard`** course workload rows; same course shows same accent across list/detail/dashboard; **narrow responsive browser viewport ~375px**; keyboard focus on course links/cards; console check for no token/secret/course-data logs) |
| **BX-I5** | **Complete** (commit **`c2288d4`**) — follow-up: authenticated material-detail visual QA when a test account exists (edit/save, unsaved blocks generate, generate/clear/restore/delete history, import tasks/flashcards, saved flashcards study, delete material danger zone, fake UUID/not found, **narrow responsive browser viewport ~375px**, console check for no token/secret/full material content logs) |
| **BX-I6B** | **Complete** (commit **`cceb4e0`**) — dashboard command-center visual upgrade on **`/dashboard`** only; authenticated manual smoke **passed**; known non-blocking: duplicate JSDoc above **`PulseMetric`**, semantic modifier hooks **`dashboard-hero--flagship`** / **`dashboard-study-pulse--cockpit`**, contrast reviewed statically/manual-smoke not lab-measured |
| **BX-I6C** | **Complete** (commit **`6a1e6ad`**) — courses / course-detail visual alignment on **`/courses`** and **`/courses/:id`** only; six approved frontend files; authenticated manual smoke **passed** with notes (`/courses` empty state not smoke-tested; empty-state double framing may be cosmetic follow-up) |
| **BX-I6D** | **Complete** (commit **`9252ba9`**) — global shell / top navigation chrome; **`layout.css`** only; authenticated **375px** shell spot-check **passed with notes** (Flashcards focus near nav edge — minor, non-blocking) |
| **B4-A** | **Complete** (commit **`4ae80ee`**) — tasks page body polish on **`/tasks`** only; three approved frontend files; native status filter **`aria-pressed`** fix (**B4-A-F1**); authenticated manual smoke **limited** — future recommended check: populated **`/tasks`**, filters, create/edit/complete/delete on safe test task, Start Focus, **narrow responsive browser viewport ~375px**, console check |
| **B4-B** | **Complete** (commit **`f91415d`**) — flashcards page body polish on **`/flashcards`** only; three approved frontend files; authenticated manual smoke **partial** — future recommended check: filter-empty/global-empty, create/edit/delete on safe test cards, delete-failure **`actionError`** visibility, **narrow responsive browser viewport ~375px**, console check |
| **B4-C** | **Complete** (commit **`cf50729`**) — Trello page body polish on **`/trello`** only; seven approved frontend files; **B4-C-F1** courses error scope fix; authenticated manual smoke **partial** — future recommended check: board/list load and sync results with safe Trello credentials, clear-credentials after sync, **narrow responsive browser viewport ~375px**, console check for no credential/task logs |
| **B4-D** | **Complete** (commit **`905ee4d`**) — admin page body polish on **`/admin`** only; two approved frontend files; authenticated manual smoke **limited** — future recommended check: admin stats load + Refresh, non-admin **`AdminRoute`** forbidden, API **403** in-page forbidden, **narrow responsive browser viewport ~375px**, console check for no token/session/admin payload logs |
| **B4-E** | **Complete** (commit **`7f4bf6b`**) — focus page body polish on **`/focus/:taskId`** only; two approved frontend files; authenticated manual smoke **limited** — future recommended check: Start Focus from pending task, starting → active timer, complete without/with checkbox on safe test task, done summary uses backend duration, Back to tasks/course, **narrow responsive browser viewport ~375px**, keyboard focus, console check for no token/session/user/focus payload logs |
| **B4-F1** | **Complete** (commit **`ea8a899`**) — shared state primitives CSS polish; one approved file (**`components.css`** only); authenticated manual smoke **limited** — future recommended check: material generate processing lane, dashboard/courses loading + error, courses/tasks/flashcards empty states, admin forbidden vs error, protected loading, **narrow responsive browser viewport ~375px**, keyboard focus on EmptyState CTA / Try again, console clean |
| **B4-F2** | **Complete** (commit **`ee50b8e`**) — route state surface framing; five approved frontend files; authenticated manual smoke **limited** — future recommended check: dashboard loading/error + **Try again**, courses loading/error/empty + **Try again**, course fake UUID not-found, material fake UUID not-found, **narrow responsive browser viewport ~375px**, keyboard focus on **Try again**/back links, console clean |
| **B4-F3A** | **Complete** (commit **`596e869`**) — secondary in-page state surfaces; **`layout.css`** only; authenticated manual smoke **limited** — future recommended check: **`/courses/:id`** materials loading/error/empty, tasks filter-empty, **`/study-materials/:id`** flashcards loading/error/empty, plan/history error if safely triggerable, **narrow responsive browser viewport ~375px**, keyboard focus on **Try again**/CTA, console clean |
| **B4-F3B** | **Complete** (commit **`ee5357f`**) — state surface wrapper hooks; four approved frontend files (**`CourseDetail.jsx`**, **`CourseTasksSection.jsx`**, **`DbFlashcardsSection.jsx`**, **`layout.css`**); presentation-only wrapper classNames + **B4-F3A** selector migration; **B4-F3A**/**B4-F3B**/**B4-F3C** all **complete**; authenticated manual smoke **limited** — future recommended QA: **`/courses/:id`** materials loading/error/empty, Try again and EmptyState CTA, Pending/Completed filter-empty, **`/study-materials/:id`** flashcards library loading/error/empty, **narrow responsive browser viewport ~375px**, keyboard focus on Try again / EmptyState CTA, console clean |
| **B4-F3C1** | **Complete** (commit **`d0393d7`**) — **TrelloTaskSelector** empty-state bug fix; one approved file (**`TrelloTaskSelector.jsx`** only); authenticated manual smoke **limited** — future recommended QA: **`/trello`** zero tasks (correct empty message, no blank heading/button, console clean), **`/trello`** with tasks (select/deselect, Select all, Clear, over-limit alert), **narrow responsive browser viewport ~375px**, keyboard focus, console clean |
| **B4-F3C2** | **Complete** (commit **`d1a3c69`**) — AI processing lane **`aria-live`** cleanup; one approved file (**`StudyMaterialDetail.jsx`** only); authenticated manual smoke **limited** — future recommended QA: study material **Generate study plan**, disabled button + panel **Processing with AI…**, single polite live region via **`LoadingState`** if testable, success path, **`generateError`** if safely triggerable, **narrow responsive browser viewport ~375px**, keyboard focus, console clean |
| **B4-F3C3** | **Complete** (commit **`ab28307`**) — **GeneratedPlanHistory** preview **`aria-live`** cleanup; one approved file (**`GeneratedPlanHistorySection.jsx`** only); **B4-F3C** sub-series (**B4-F3C1**, **B4-F3C2**, **B4-F3C3**) **complete**; authenticated manual smoke **limited** — future recommended QA: study material with generated plan history → Preview inactive version → **Loading preview…** visible (polite announcement if testable) → snippet + meta only on success → **`previewError`** if safely triggerable → keyboard on Preview / Make active / Delete → **narrow responsive browser viewport ~375px** → console clean |
| **BX-I7B** | **Complete** (commit **`00d3255`**) — global desktop cockpit shell widening; **`tokens.css`** + **`layout.css`** only; **`--content-max-cockpit`** / **`--content-max-shell`** **1280px**; **foundation only** — authenticated visual smoke **recommended** (limited at gate) |
| **BX-I7C** | **Complete** (commit **`583922d`**) — dashboard desktop grid expansion; **`layout.css`** only; hero + study pulse side-by-side at **≥1280px**; 2-column course list; loading/error unaffected; **375px** stacked; **no** **`DashboardStub.jsx`** or API/service/context/recommendation/copy changes; **not** final Stitch-level UI — authenticated dashboard visual smoke **recommended before merge** if not yet done |
| **BX-I7D Tier 1** | **Complete** (commit **`52c68ab`**) — courses/course detail desktop shelf expansion; **`layout.css`** only; **`/courses`** 3-column shelf + **`/courses/:id`** 2-column document shelf at **≥1280px**; loading/error/empty unaffected; tasks below materials unchanged; **375px** stacked; **no** **`CoursesList.jsx`**, **`CourseDetail.jsx`**, component, API/service/context/recommendation/copy changes; **Tier 2** **not implemented**; **not** final Stitch-level UI — authenticated visual smoke **recommended before merge** if not yet done |
| **BX-I7D Tier 2** | **Not started** — optional materials | tasks side-by-side workspace; requires explicit approval |
| **BX-I7E1** | **Complete** (commit **`d0db43e`**) — Tasks desktop panels; **`layout.css`** only; **`/tasks`** horizontal command-controls band + **2-column** populated task list at **≥1280px**; create FormCard full-width; inline edit intended full width via **`:has(.task-workspace__edit-card)`**; loading/error/empty/filter-empty unaffected; **375px** stacked; **no** **`GlobalTasksSection.jsx`** / **`TasksPage.jsx`** / **`TaskCard.jsx`** or API/service/context/copy changes; **not** final Stitch-level UI — inline edit / console smoke **recommended before merge** if not yet done |
| **BX-I7E2** | **Complete** (commit **`b18304c`**) — Flashcards desktop panels; **`layout.css`** only; **`/flashcards`** horizontal command-controls band + **study | manage** **2-column** populated library + **2-column** manage list at **≥1280px**; create/inline edit full-width; loading/error/empty/filter-empty/**`actionError`** unaffected; manage list truncated questions only — **no** answer exposure; **375px** stacked; **no** **`GlobalFlashcardsSection.jsx`** / **`FlashcardsPage.jsx`** / **`FlashcardStudy.jsx`** or API/service/context/copy changes; **not** final Stitch-level UI — loading/error/**`actionError`** / console / keyboard tab smoke **recommended before merge** if not yet done (optional) |
| **BX-I7E3 Tier A** | **Complete** (commit **`cba6dde`**) — Trello desktop setup panels; **`layout.css`** only; **`/trello`** Step 1 credentials + Step 2 destination side-by-side at **≥1280px**; Step 3 tasks, messages, Step 4 sync full-width; results below command band; DOM order unchanged; **375px** stacked; **no** Trello JSX/components/services/API/backend changes; **Tier B not implemented**; **not** final Stitch-level UI — authenticated visual smoke **recommended before merge** if not yet done (optional) |
| **BX-I7E3 Tier B** | **Not started** — proposed Trello desktop Tier B (task-list grid, results grid, wizard/results side-by-side, scroll-height override); requires explicit approval |
| **BX-I7E4** | **Complete** (commit **`467ccd9`**) — Admin desktop stats panels; **`layout.css`** only; stats deck **2-column** grid at **≥1280px**; Platform overview, Trello today, Backend status full-width; Tasks|Focus and Learning|Trello stats rows side-by-side; inner stat grids unchanged; loading/error/forbidden/**`AdminRoute`** unaffected; **375px** stacked; **no** JSX/API/backend/services/auth/copy changes; **not** final Stitch-level UI — authenticated visual smoke **recommended before merge** if not yet done (optional) |
| **BX-I7F** | **Complete** (commit **`25988dc`**) — material cockpit desktop pass; **`layout.css`** only; cockpit **1.15fr \| 0.85fr** at **≥1280px**; plan-history/import actions horizontal; library **study \| manage** **2-column**; **375px** stacked; **`/flashcards`** unaffected; authenticated visual smoke **recommended before merge** if not yet done (optional) |
| **BX-I8B** | **Complete** (commit **`bda2645`**) — AI command surfaces polish on **`/study-materials/:materialId`** only; **`components.css`** + **`layout.css`** only; stronger violet/glass on AI command column (generate panel, loading lane, plan output, history, import bands); **Generate** CTA remains electric blue / primary; **375px** stacked; **BX-I7F** layout preserved; **no** `tokens.css`, JSX, API, services, backend, auth, routes, behavior, copy, logging, or content-exposure changes; authenticated visual smoke **recommended before merge** if not yet done (optional — does **not** block commit) |
| **BX-I8C** | **Complete** (commit **`8008dc1`**) — Auth + PageHeader intro chrome; **`components.css`** + **`layout.css`** only; auth **`.page--auth .form-card`** glass/command-center card; intro **`PageHeader`** glass band + top gradient (**.page-header--intro** only); course accent header preserved; **375px** stacked; **BX-I7** / **BX-I8B** layouts preserved; **no** `tokens.css`, **`base.css`**, JSX, API, backend, auth, routes, behavior, copy, logging changes; focus-visible clipping smoke **recommended but not blocking** |
| **BX-I8D** | **Complete** (commit **`51cdc77`**) — Motion micro-pass; **`components.css`** + **`base.css`** only; processing pulse on real AI Processing Lane only; one-shot success/preview/focus-done entrances; reduced-motion guards; **no** JSX/JS/API/backend/routes/tokens; manual smoke **recommended before merge**, **not blocking** |
| **BX-I8E** | **Complete** (commit **`52b7b78`**) — Shared controls / card surfaces; **`components.css`** only; **`.form-card`** / **`.source-card`** baseline glass-border alignment; **`.link-btn`** / **`.alert--success`** parity; **`layout.css`** / **`tokens.css`** / **`base.css`** untouched at **BX-I8E** time (radius tokens updated later in **BX-I9B1**); optional scope **not implemented**; Visual Style Guide v2.2 check **passed**; manual smoke **recommended before merge**, **not blocking** |
| **BX-I9B1** | **Complete** (commit **`9ec2917`**) — Radius token alignment; **`tokens.css`** + **`components.css`** only; Stitch Round Eight **`--radius-sm`**/**`--radius-md`**; buttons/link-buttons **`--radius-md`**; fields **`--radius-sm`**; **no** color tokens; cascade to pills/cards **expected**; authenticated visual smoke **recommended before merge**, **not blocking** |
| **BX-I9B2a** | **Complete** (commit **`f92cbda`**) — Canvas/shell color token alignment; **`tokens.css`** only; **`--color-bg`** **`#0F172A`**; **`--color-bg-subtle`** / **`--color-bg-auth`** / **`--color-shell-bg`** / **`--color-shell-border`** harmonized; **no** radius or accent-token changes; **`components.css`** / **`layout.css`** / **`base.css`** untouched; cascade via **`layout.css`** vars **expected**; authenticated visual smoke **recommended before merge**, **not blocking** |
| **BX-I9B2b** | **Complete** (commit **`b4d7b93`**) — Primary/cyan color token alignment; **`tokens.css`** only; Stitch **`#3B82F6`** primary + **`#06B6D4`** cyan/data; fill **`#2563EB`** / **`#1D4ED8`** (**not** **`#3B82F6`** on fill); focus/glow/chart tokens harmonized; **no** canvas/shell/AI/violet/danger/radius changes; cascade **expected**; visual smoke **recommended before merge** (links on glass, hero eyebrow, focus rings) — **not blocking** |
| **BX-I9B2c** | **Complete** (commit **`19d444e`**) — AI/violet color token alignment; **`tokens.css`** + **`components.css`** + **`layout.css`** only; Stitch **`#8B5CF6`**; mechanical lavender sweep; **Generate** CTA remains primary blue; visual smoke **recommended before merge** — **not blocking** |
| **BX-I9B2d** | **Complete** (commit **`ae5cfc8`**) — danger/error color token alignment; **`tokens.css`** only; Stitch Error / Rose Red **`#E11D48`**; fill **`#be123c`** / error copy **`#fda4af`**; course rose accents **unchanged**; merge-time danger/error visual QA **completed** in **BX-I9C** / **BX-I9C-Auth** — **Pass with notes** |
| **BX-I9C** | **Complete** — **Pass with notes** | Final visual QA after **BX-I9B** token alignment — **review only**; auth routes + **BX-I9C-Auth** authenticated matrix on **`http://localhost:5173`** at **~375px** / **1280px** / **1440px**; **no** repo changes; **not** final Stitch-perfect |
| **BX-I9C-Auth** | **Complete** — **Pass with notes** | Authenticated route matrix; runtime test data in Supabase only (see phase table) |
| **Remaining B4 / remaining BX-I6** | **Not started** — sidebar, chart UI/APIs, and backend/API extension each require separate planning and explicit **`approved — implement Phase X`** |

**Human next step (typical):** **BX-I10A5** targeted glass/elevation pass is **complete** (commit **`38aa561`**). **BX-I10A6-QA** Trello failed-row live verification is **complete** (**Pass with notes**, review only — **no** fix phase required). Optional **separate-gated** work only: optional **BX-I7D Tier 2** / **BX-I7E3 Tier B**; optional **`flashcard-study--plan`** live QA after generating a plan with flashcards; optional material detail course accent JSX if explicitly approved; optional material editor/library elevation alignment; optional global shadow/token cleanup only if explicitly approved; optional top-nav narrow viewport shell polish only if separately approved; **ROADMAP-A1** background automations + AWS architecture planning; remaining **B4** / **BX-I6** work (sidebar, chart UI/APIs) — each requires explicit **`approved — implement Phase X`**. **Do not** start Trello failed-row **implementation** fixes, material detail course accent JSX, chart libraries, sidebar shell, backend/API extension, or new color-token phases without a separately approved phase gate. **BX-I10A1** through **BX-I10A6-QA**, **B4-F3A** / **B4-F3B** / **B4-F3C**, **BX-I7B** through **BX-I7F**, **BX-I8B** through **BX-I8E**, **BX-I9B1**, **BX-I9B2a**–**BX-I9B2d**, and **BX-I9C** / **BX-I9C-Auth** are **complete** — next **implementation** phase is **not automatic**.

---

## Source-of-truth hierarchy

When documents disagree, resolve in this order:

1. **`docs/IMPLEMENTATION_STATUS.md`** + application code + `supabase/migrations/` — **what is shipped**
2. **`docs/CURRENT_STATE.md`** (this file) — **where we are in phases** and what is suspended
3. **`docs/PRD.md`** — product vision, target MVP, **future / not-yet-built** features
4. **`docs/adrs/*`** — architecture boundaries
5. **`SECURITY.md`** — trust boundaries and Security Review triggers
6. **`AGENTS.md`** / **`docs/workflows/*`** — process and approvals
7. **`docs/AGENT_MEMORY.md`** — historical journal + pitfalls (append-only; must not override 1–4)
8. **`DESIGN.md`** + **`tokens.css`** — presentation (DESIGN = direction; tokens = live values)
9. **Stitch / prototype / screenshots** — inspiration only

Conflict workflow: `docs/workflows/conflict-resolution-workflow.md`

---

## Forbidden assumptions

Do **not** assume without checking **`IMPLEMENTATION_STATUS`** and this file:

| Assumption | Reality |
|------------|---------|
| PRD section describes live behavior | PRD includes **future scope** — verify shipped state |
| `DESIGN.md` or BX-I1 doc update = implement approval | Requires **`approved — implement Phase X`** |
| Stitch exports are production code | **Reference only** — never merge into `frontend/src` |
| Course-level generate with client `studyText` exists | **Deferred** — material-scoped generate with body `{}` only |
| Trello OAuth account connection is live | **Yes (A4-FRONTEND)** — Connect/Disconnect on **`/trello`**; protected **`/trello/connect/callback`**; backend **A3** + **A4-STATE** reviewed |
| Trello sync uses stored OAuth token | **Yes (A5A + A5B)** — when connected, **`/trello`** frontend sends `{}` / `{ listId, taskIds }` only; backend uses decrypted stored token. **Manual fallback** when disconnected still sends ephemeral `{ apiKey, token }` (ADR 004) |
| Trello connect/disconnect HTTP routes | **Live** — `GET /api/trello/connection`, `GET /api/trello/authorize-url`, `POST /api/trello/connect/complete` (`{ token, state }`), `POST /api/trello/disconnect` — frontend uses these via **`trello.service.js`** only |
| Trello OAuth signed state | **A4-STATE shipped** — HMAC-signed state on authorize-url / connect/complete blocks account-linking CSRF; state is stateless, not single-use; replay within 10-minute TTL is accepted MVP residual risk |
| `trello_connections` is unused in production routes | **Incorrect** — **A3** writes encrypted tokens on connect/complete; **A5A** reads/decrypts for boards/lists/sync; **A5B** frontend uses stored mode when connected |
| Manual apiKey/token is the only `/trello` sync path | **Incorrect since A5B** — connected users sync via stored token; manual fallback exists when disconnected |
| Trello tokens safe in frontend localStorage | **Never** — manual credentials cleared from React state; OAuth tokens must **never** be stored client-side |
| Admin logs / user list / role UI | **Not implemented** — aggregate stats only |
| Dashboard charts / sidebar | **Documented direction only** — **not wired in code**; dark token foundation live via **BX-I2**; rule-based decision hero live via **BX-I3** |
| Course accents not wired | **BX-I4** shipped deterministic accents on **`/courses`**, **`/courses/:id`**, and dashboard workload rows — **`amber` | `rose` | `emerald`** from course ID/name/title only; supplementary chrome only; **no** DB persistence |
| Dashboard hero implies AI recommendation | **BX-I3** hero is **rule-based** only — derived from existing **`GET /api/dashboard/stats`** counts; **no** AI recommendation API |
| Course accent implies health / priority / urgency | **BX-I4** accents are **visual chrome only** — **no** health score, priority, urgency, active/quiet state, or AI classification |
| B4 already approved | **Partial** — **B4-A** **`/tasks`**, **B4-B** **`/flashcards`**, **B4-C** **`/trello`**, **B4-D** **`/admin`**, **B4-E** **`/focus/:taskId`**, **B4-F1** shared primitives polish, **B4-F2** route state framing, **B4-F3A** secondary in-page state surfaces, **B4-F3B** state surface wrapper hooks, **B4-F3C1** **TrelloTaskSelector** empty-state fix, **B4-F3C2** AI lane **`aria-live`** cleanup, and **B4-F3C3** **GeneratedPlanHistory** preview **`aria-live`** cleanup are **complete** (commits **`4ae80ee`**, **`f91415d`**, **`cf50729`**, **`905ee4d`**, **`7f4bf6b`**, **`ea8a899`**, **`ee50b8e`**, **`596e869`**, **`ee5357f`**, **`d0393d7`**, **`d1a3c69`**, **`ab28307`**); **B4-F3A** / **B4-F3B** / **B4-F3C** all **complete**; remaining **B4** global styling rollout is **not started**; each sub-phase requires explicit approval |
| B4-A complete = B4-B or other page-body polish approved | **B4-A** was **`/tasks`** page body presentation only — three approved files; **B4-B**, **B4-C**, **B4-D**, **B4-E**, **B4-F1**, and **B4-F2** are **complete** separately; **B4-F3**, sidebar, charts, and backend/API extension need separate gates |
| B4-B complete = B4-C or other page-body polish approved | **B4-B** was **`/flashcards`** page body presentation only — three approved files; **B4-C**, **B4-D**, **B4-E**, **B4-F1**, and **B4-F2** are **complete** separately; **B4-F3**, sidebar, charts, and backend/API extension need separate gates |
| B4-C complete = B4-D or other page-body polish approved | **B4-C** was **`/trello`** page body presentation only — seven approved files; **B4-D**, **B4-E**, **B4-F1**, and **B4-F2** are **complete** separately; **B4-F3**, sidebar, charts, and backend/API extension need separate gates |
| B4-D complete = focus/shared-state polish approved | **B4-D** was **`/admin`** page body presentation only — two approved files; **B4-E**, **B4-F1**, and **B4-F2** are **complete** separately; **B4-F3**, sidebar, charts, and backend/API extension need separate gates |
| B4-E complete = shared-state polish approved | **B4-E** was **`/focus/:taskId`** page body presentation only — two approved files; **B4-F1** and **B4-F2** are **complete** separately; **B4-F3**, sidebar, charts, and backend/API extension need separate gates |
| B4-F1 complete = B4-F2 or B4-F3 approved | **B4-F1** was shared state primitives CSS polish only — one approved file (**`components.css`**); **B4-F2**, **B4-F3A**, **B4-F3B**, and **B4-F3C** (**B4-F3C1**, **B4-F3C2**, **B4-F3C3**) are **complete** separately; sidebar, charts, and backend/API extension need separate gates |
| B4-F2 complete = B4-F3 approved | **B4-F2** was route state surface framing only — five approved files; **B4-F3A**, **B4-F3B**, and **B4-F3C** sub-series are **complete** separately; sidebar, charts, and backend/API extension need separate gates |
| B4-F3A complete = B4-F3B or B4-F3C approved | **B4-F3A** was secondary in-page state surface CSS polish only — one approved file (**`layout.css`**); **B4-F3B** wrapper hooks (**`ee5357f`**) and **B4-F3C** sub-series (**B4-F3C1**, **B4-F3C2**, **B4-F3C3**) are **complete**; sidebar, charts, and backend/API extension need separate gates |
| B4-F3B complete = next B4 phase approved | **B4-F3B** was state surface wrapper hooks only — four approved files; **B4-F3A**/**B4-F3B**/**B4-F3C** all **complete**; next implementation phase is **not automatic** |
| B4-F3C1 complete = B4-F3C2 or B4-F3C3 approved | **B4-F3C1** was isolated **TrelloTaskSelector** zero-tasks empty-state bug fix only — one approved file; **B4-F3C2** and **B4-F3C3** are **complete** separately; **B4-F3C** sub-series **complete**; **B4-F3A**/**B4-F3B**/**B4-F3C** all **complete**; next implementation phase is **not automatic** |
| B4-F3C2 complete = B4-F3C3 approved | **B4-F3C2** was isolated AI processing lane duplicate **`aria-live`** cleanup only — one approved file; **B4-F3C3** is **complete** separately (commit **`ab28307`**, **`GeneratedPlanHistorySection.jsx`** only); **B4-F3C** sub-series **complete**; next implementation phase is **not automatic** |
| B4-F3C3 complete = next B4 phase approved | **B4-F3C3** was isolated **GeneratedPlanHistory** preview **`aria-live`** cleanup only — one approved file; **B4-F3A**/**B4-F3B**/**B4-F3C** all **complete**; next implementation phase is **not automatic** and requires separate planning + explicit approval |
| “Platform control” on `/admin` implies user/role management | **B4-D** **“Platform control”** is approved read-only aggregate UI framing only — **not** user/role/logs management; optional copy softening non-blocking per Security / Trust Review |
| Material cockpit polish not wired | **BX-I5** + **BX-I8B** shipped visual polish on **`/study-materials/:materialId`** only — **no** behavior/API change; course accents on material detail **not shipped** |
| `AGENT_MEMORY` tail entry is current | Read **this file** and **`IMPLEMENTATION_STATUS`** first |
| BX-I6C complete = B4 or page-body polish approved | **BX-I6C** was **`/courses`** + **`/courses/:id`** visual presentation only; **B4-A** through **B4-F3B** and **B4-F3C1**–**B4-F3C3** are **complete** separately; **B4-F3A**/**B4-F3B**/**B4-F3C** all **complete**; sidebar and chart UI/APIs need separate gates |
| BX-I6D complete = B4 or page-body polish approved | **BX-I6D** was global shell chrome (**`layout.css`** only) — **no** route fade, page transitions, or auth changes; **B4-A** through **B4-F3B** and **B4-F3C1**–**B4-F3C3** are **complete** separately; remaining **B4** / **BX-I6** work needs separate gate |
| “Flashcard library” on `/flashcards` implies AI automation | **B4-B** **“Flashcard library”** / **“Filter, study, and manage saved cards”** are factual **UI framing only** — **not** AI/automation claims; deck/command styling is visual chrome only |
| “Task command” on `/tasks` implies AI automation | **B4-A** **“Task command”** is approved **UI framing only** — **not** an AI/automation claim; **`task.priority`** remains API-backed only |
| Dashboard BX-I6B implies new recommendation logic | **BX-I6B** changed presentation/CSS only — **`dashboard-recommendation.js`** unchanged; hero remains **rule-based** |
| 375px / “mobile” in QA notes | Means **narrow responsive browser viewport** testing only — **not** native mobile app scope |
| Native mobile / app-store assumed from Stitch or DESIGN | **Web UI reference only** — native mobile, sidebar, and app-store patterns require separate explicit approval |
| `IMPLEMENTATION_STATUS` “Last aligned” before **12A-1** / **B1–B3** | Fixed in **DOCS-A3** — if lag recurs, verify code + **`AGENT_MEMORY`** |
| Screenshots match live UI | Many PNGs **predate 8C / 11A-3 / B1–B3** — trust code + status docs |
| B4 / BX-I6 / BX-I7 / BX-I8B polish complete = UI is final Stitch-level | **B4**/**BX-I6**/**BX-I7**/**BX-I8B** improved consistency and material AI command surfaces — **not** final Stitch desktop SaaS layout; optional **BX-I7D Tier 2** / **BX-I7E3 Tier B** **not implemented** |
| BX-I2 / BX-I6 / BX-I7 / BX-I8B = full Stitch design in production | Tokens, page polish, global **1280px** shell, per-route desktop density (**BX-I7C**–**BX-I7F**), and material AI command surfaces (**BX-I8B**) shipped — **Stitch references are not fully reflected** on all routes yet |
| Desktop cockpit already uses full desktop web layout | Hub routes use **`page--cockpit`** at **1280px** max (**BX-I7B**); per-route desktop density **BX-I7C**–**BX-I7F** shipped; material AI command surfaces polished **BX-I8B** — UI improved but **not** Stitch-perfect on all routes |
| Future styling is only minor polish | **BX-I9B2** color token sequence + **BX-I9C** final visual QA are **complete** (**Pass with notes**); remaining visual work is optional polish (AI Generate gradient, **flashcard-study** glass, glass/elevation pass, **BX-I7D Tier 2** / **BX-I7E3 Tier B**, charts, sidebar, fonts) — each needs explicit approval — **not** claimed Stitch-perfect |
| BX-I8B complete = BX-I8C/D/E approved | **BX-I8B** was AI command surfaces **CSS-only** polish — **BX-I8C**, **BX-I8D**, and **BX-I8E** each required separate approval (now **complete** at **`8008dc1`**, **`51cdc77`**, **`52b7b78`**) |
| BX-I8C complete = BX-I8D/E approved | **BX-I8C** was Auth + PageHeader intro **CSS-only** polish — **BX-I8D** and **BX-I8E** required separate approval (now **complete** at **`51cdc77`** and **`52b7b78`**) |
| BX-I8D complete = BX-I8E approved | **BX-I8D** was Motion micro-pass **CSS-only** — **BX-I8E** required separate approval (now **complete** at **`52b7b78`**) |
| BX-I8E complete = token/radius or gradient phases approved | **BX-I8E** was shared controls / card surfaces **`components.css`** only — radius in **BX-I9B1** (**`9ec2917`**); **BX-I9B2a**–**BX-I9B2d** each required separate approval (now **complete**); AI Generate gradient and **flashcard-study** polish require separate planning + explicit approval |
| BX-I9B1 complete = BX-I9B2 color or gradient phases approved | **BX-I9B1** was radius tokens + button/link-button radius only — **no** color token changes; **BX-I9B2a**–**BX-I9B2d** each required separate approval (now **complete**); AI Generate gradient and **flashcard-study** polish require separate planning + explicit approval |
| BX-I9B2a complete = BX-I9B2b/c/d or gradient phases approved | **BX-I9B2a** was canvas/shell environment color tokens in **`tokens.css`** only — **no** primary/AI/cyan changes; **BX-I9B2b**–**BX-I9B2d** each required separate approval (now **complete**); AI Generate gradient and **flashcard-study** polish require separate planning + explicit approval |
| BX-I9B2b complete = BX-I9B2c/d or gradient phases approved | **BX-I9B2b** was primary/cyan/data/chart/focus/glow-primary tokens in **`tokens.css`** only — **no** AI/violet/danger/canvas/shell/radius changes; **BX-I9B2c**–**BX-I9B2d** each required separate approval (now **complete**); AI Generate gradient and **flashcard-study** polish require separate planning + explicit approval |
| BX-I9B2c complete = BX-I9B2d or gradient phases approved | **BX-I9B2c** was AI/violet tokens in **`tokens.css`** + mechanical lavender sweep in **`components.css`** / **`layout.css`** only — **no** danger/error/primary/cyan/radius/canvas/shell changes; **Generate** CTA remains primary blue; **BX-I9B2d** required separate approval (now **complete** at **`ae5cfc8`**); AI Generate gradient and **flashcard-study** polish require separate planning + explicit approval |
| BX-I9B2d complete = gradient or polish phases approved | **BX-I9B2d** was danger/error tokens in **`tokens.css`** only — visual QA **completed** in **BX-I9C** / **BX-I9C-Auth** (**Pass with notes**); AI Generate gradient, **flashcard-study** polish, glass/elevation pass, sidebar, and chart UI require separate planning + explicit approval |
| BX-I9C complete = final-perfect or new polish approved | **BX-I9C** was **review-only** visual QA — **Pass with notes**, **not** final Stitch-perfect; **no** implementation changes; optional polish phases remain **separate-gated** |
| BX-I7F complete = BX-I8B approved | **BX-I7F** was material cockpit **`layout.css`** desktop pass only — **BX-I8B** required separate implement approval (now **complete** at **`bda2645`**) |
| BX-I7B complete = BX-I7C/D/E/F approved | **BX-I7B** was **CSS/tokens foundation only** — **BX-I7C**, **BX-I7D Tier 1**, **BX-I7E1**, **BX-I7E2**, **BX-I7E3 Tier A**, and **BX-I7E4** each required separate implement approval; **BX-I7F**, optional **BX-I7D Tier 2**, and **BX-I7E3 Tier B** require separate planning + explicit approval each |
| BX-I7C complete = BX-I7D/E/F approved | **BX-I7C** was **dashboard `layout.css` grid only** — **BX-I7D Tier 1**, **BX-I7E1**, **BX-I7E2**, **BX-I7E3 Tier A**, and **BX-I7E4** required separate implement approval; **BX-I7F**, optional **BX-I7D Tier 2**, and **BX-I7E3 Tier B** require separate planning + explicit approval each |
| BX-I7D Tier 1 complete = BX-I7D Tier 2 / BX-I7E / BX-I7F approved | **BX-I7D Tier 1** was **courses/course detail `layout.css` shelf grids only** — **Tier 2** side-by-side workspace **not implemented**; **BX-I7E1** (**`d0db43e`**), **BX-I7E2** (**`b18304c`**), **BX-I7E3 Tier A** (**`cba6dde`**), and **BX-I7E4** (**`467ccd9`**) each required separate approval; **BX-I7F** requires separate planning + explicit approval |
| BX-I7E1 complete = BX-I7E2/E3/E4/F approved | **BX-I7E1** was **`/tasks` `layout.css` desktop panels only** — **BX-I7E2** required separate approval (now **complete** at **`b18304c`**); **BX-I7E3 Tier A** required separate approval (now **complete** at **`cba6dde`**); **BX-I7E4** required separate approval (now **complete** at **`467ccd9`**); **BX-I7F** requires separate planning + explicit approval |
| BX-I7E2 complete = BX-I7E3/E4/F approved | **BX-I7E2** was **`/flashcards` `layout.css` desktop panels only** — **BX-I7E3 Tier A** required separate approval (now **complete** at **`cba6dde`**); **BX-I7E4** required separate approval (now **complete** at **`467ccd9`**); **BX-I7F** requires separate planning + explicit approval |
| BX-I7E3 Tier A complete = BX-I7E3 Tier B / BX-I7E4 / BX-I7F approved | **BX-I7E3 Tier A** was **`/trello` `layout.css` desktop setup panels only** — credentials + destination 2-up at **≥1280px**; **Tier B** (task-list grid, results grid, wizard/results side-by-side, scroll-height override) **not implemented**; **BX-I7E4** required separate approval (now **complete** at **`467ccd9`**); **BX-I7F** requires separate planning + explicit approval |
| BX-I7E4 complete = BX-I7F approved | **BX-I7E4** was **`/admin` `layout.css` desktop stats deck only** — stats deck **2-column** grid at **≥1280px**; Platform overview, Trello today, Backend status full-width; Tasks|Focus and Learning|Trello stats rows side-by-side; inner stat grids unchanged; **no** JSX/API/backend/services/auth/copy changes; **BX-I7F** requires separate planning + explicit approval |
| BX-I7A complete = BX-I7B approved | **BX-I7A** was **docs-only** audit — **BX-I7B** required separate implement approval (now **complete** at **`00d3255`**) |

**Operating constraints:** Free Tier / minimal cost; no new paid APIs or npm packages without approval; Gemini **429** = quota, not necessarily a bug.

---

## Quick links

| Doc | Use for |
|-----|---------|
| [`IMPLEMENTATION_STATUS.md`](IMPLEMENTATION_STATUS.md) | Routes, APIs, env, DB, deferred work |
| [`POST_MVP.md`](POST_MVP.md) | Post-MVP framing — initial MVP closed; future work still gated |
| [`PRD.md`](PRD.md) | Product vision and target MVP (includes future scope) |
| [`AGENT_MEMORY.md`](AGENT_MEMORY.md) | Phase history and pitfalls |
| [`../DESIGN.md`](../DESIGN.md) | Presentation spec |
| [`../AGENTS.md`](../AGENTS.md) | Agent approvals and DoD |
| [`../SECURITY.md`](../SECURITY.md) | Security boundaries |
